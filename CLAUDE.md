# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project status

Implementation has started, following [Projeto_detalhado/PROJETO_AUDIOBOOK_PLANO_DETALHADO.md](Projeto_detalhado/PROJETO_AUDIOBOOK_PLANO_DETALHADO.md) (full 20-week plan: architecture, DB schema, API routes, pricing, commit/branch conventions). **Etapa 1 / Semana 1-2 (Dias 1-10) is done**: monorepo scaffold, backend (Express + PostgreSQL, DB schema/migrations, auth/audiobook/progress endpoints, JWT), web (React + Vite + React Router + Tailwind v4, layout, auth pages, protected routes), desktop (Electron reusing `web/`'s React, IPC handlers, SQLite offline cache, auto-sync with backend), and CI (GitHub Actions test/lint/deploy-staging, Husky pre-commit). **Etapa 2 / Semana 3, Dia 11-12 is done**: audio player component (`packages/web/src/components/features/AudioPlayer.jsx` — play/pause, seek, volume, speed control) wired into `PlayerPage`, which fetches chapters per audiobook and persists listening progress via the existing `/api/user/progress` endpoints. `words` gained `start_seconds`/`end_seconds` (migration 002) and a `GET /api/audiobooks/chapters/:chapterId/words` endpoint; `useWordSync` + `TranscriptDisplay` highlight the word matching current playback time, falling back to plain `chapter.transcript` when no timestamped words exist yet. **Dia 13-14 is done**: clicking a transcript word opens `TranslationPopup` (translation, pronunciation, technical explanation, example sentence), auto-closing after 3s, and also POSTs to `/api/user/words-learned` — this increments `user_progress.words_learned` for the chapter and logs the event to a new `word_clicks` table (migration 003) for future analytics (difficulty analysis, Dia 31). **Dia 15 (manual part) is done**: full flow verified against a real local Postgres — register → login → audiobooks list → player → real audio playback → word highlighting → translation popup → word-click and completion progress persisted correctly. Automated e2e tests (Playwright/Vitest) are not set up yet. **Dia 16 is done**: a `technical_dictionary` table (migration 004, seeded with 24 real terms in 005) provides canonical part-of-speech/translation/explanation/example/contexts per word, standalone at `GET /api/dictionary/:word`. `getChapterWords` now LEFT JOINs it so any per-chapter `words` row missing translation/explanation/example falls back to the dictionary entry; `part_of_speech`/`contexts` always come from there since chapter-level `words` doesn't store them. `TranslationPopup` displays both. **Dia 17 is done**: clicking a word now also auto-creates a `flashcards` row (`ON CONFLICT DO NOTHING`). SM-2 spaced repetition lives in `packages/backend/src/services/srsService.js` (pure function, unit-testable), applied via `POST /api/user/flashcards/:id/review` `{quality: 0-5}`. `FlashcardReviewPage` (`/flashcards`) shows one due card at a time, reveal-then-rate. Verified against real Postgres — caught and fixed a real bug ([c1beed7](https://github.com/andersunrise1/audiobooks-business/commit/c1beed7)) where Postgres couldn't infer a type for a param used both as `int` and concatenated into an interval string; fixed with `make_interval(days => $2::int)`. **Dia 18 is done**: `PronunciationRecorder` (below the transcript in `PlayerPage`) uses the browser's Web Speech API (`SpeechRecognition`/`webkitSpeechRecognition`, no Deepgram yet) to record and transcribe the user reading a chapter's sentence, then `utils/pronunciationScore.js` gives a rough word-overlap score (0-100%) with matched/unmatched words highlighted green/red. Falls back to a "not supported" message if the browser lacks the API; mic-permission errors are caught and shown in Portuguese. **Dia 19 is done**: `GET /api/user/stats` (words learned today/week/month from `word_clicks`, flashcards due, approximate total study time from `chapters.duration_seconds * user_progress.listening_count`, day streak via `statsService.computeStreakDays` — pure function, unit-tested with several gap/edge scenarios — and audiobooks in progress). `DashboardPage` renders it with `StatCard`/`AudiobookProgressList`. Verified against real data end-to-end. **Dia 20 is done, closing Etapa 2 / Semana 4**: `packages/web/src/services/desktopBridge.js` wraps `window.techspeak` (undefined/no-op outside Electron) — `AuthContext` now pushes the session into the desktop main process on login/logout (previously dead code, since nothing ever called `auth:setSession`), and `PlayerPage.saveProgress` falls back to `queueDesktopProgress` when the API call fails. Offline audio playback: `packages/desktop/public/audioCache.js` downloads a chapter's audio to `userData/audio-cache/` and records it in a new `audio_cache` SQLite table (`db.js`); `PlayerPage` resolves a chapter's audio `src` through `cache.getCachedAudioPath` first (falling back to the remote URL and kicking off a background download when on desktop). Verified: the download/cache logic works end-to-end via a plain-Node functional test (real download, cache hit on 2nd call); the web-side fallback (no `window.techspeak`) was verified in-browser to behave exactly as before. **Not verified: an actual packaged/running Electron window** — these tools only drive a regular browser tab against the Vite dev server, not `electron .`; the IPC wiring itself is untested end-to-end inside Electron.

**Dia 21 is done**: backend testing uses Node's built-in test runner (`node --test`, zero new dependencies) instead of Vitest — `packages/backend/src/index.js` was split into `app.js` (`createApp()` factory, importable without binding a port) + a thin `index.js` entrypoint. `tests/unit/` covers `srsService`/`statsService` pure functions; `tests/integration/` (`auth.test.js`, `player.test.js`, matching the plan's exact filenames) spins up a real server on an ephemeral port against the actual local Postgres and cleans up its fixtures afterward. `npm run test:coverage --workspace=packages/backend` runs `--experimental-test-coverage`; CI's `test.yml` now posts the coverage table to the GitHub Actions job summary. Also fixed along the way: CI's `lint` job (which runs `prettier --check`) had been silently failing since Dia 11 or so because the Husky pre-commit hook only ran `eslint`, never `format:check` — added it to pre-commit and reformatted the 3 affected files.

**Dia 22 is done**: `packages/web` now has Vitest + React Testing Library (`npm test` → `vitest run`; `npm run test:watch` for local dev). Component tests cover `pronunciationScore`, `useWordSync`, `StatCard`, `TranscriptDisplay`; integration tests (`LoginPage.test.jsx`, `PlayerPage.test.jsx`) mock `services/api.js` and render through the real `AuthProvider` + React Router, exercising the exact login and word-click flows verified manually in-browser earlier this project — 19 tests total, all passing.

**Dia 23 is done, scoped to what's real at this stage**: `AudioPlayer`'s `<audio>` now uses `preload="metadata"` (don't download full audio until played); every route except `/` is `React.lazy()`-loaded behind a `Suspense` boundary; `vite.config.js` splits React/ReactDOM/React Router into a dedicated `vendor` chunk via `manualChunks` (note: Vite 8's Rolldown bundler requires `manualChunks` as a function, not the classic Rollup object form). All three verified against a real production build — per-route and vendor chunks confirmed loading independently via network requests. The plan's other two Dia 23 items ("caching de imagens", "compressão de áudio") were skipped honestly: there's no cover-image field/UI yet and no audio upload/processing pipeline (that's Etapa 4, Dia 43+) — nothing real to optimize yet.

**Dia 24 is done**: fixed a real mobile bug — `Sidebar` (with the `Flashcards` link) is `hidden` below the `sm` breakpoint with no replacement, so mobile users had no way to reach `/flashcards`; `Navbar` now carries all primary links itself and wraps (`flex-wrap`) instead of overflowing on narrow screens. `Layout`'s main area got `min-w-0` (prevents flex children from forcing horizontal scroll) and tighter mobile padding. Touch targets were measured for real at 375px width via `getBoundingClientRect()` and enlarged where under ~40px: play button 40→44px, speed buttons 28→36px tall, transcript word spans 26→40px tall; `touch-manipulation` added throughout to drop the ~300ms tap delay. Verified visually at mobile (375×812) and tablet (768×1024) via `resize_window` on the home, dashboard, and player pages.

**Dia 25 is done, closing Etapa 2 (Semanas 3-5, Dias 11-25) — full audiobook player + translation phase**. Two real bugs found by re-reading the accumulated code rather than writing new features: (1) `AudioPlayer` kept playback speed/volume in its own state, but `PlayerPage` remounts it (`key={chapter.id}`) on every chapter change to reset position/duration — so speed/volume silently reset to defaults each time; both are now lifted into `PlayerPage` and passed down as controlled props (verified: 1.5x survives a chapter switch in-browser). (2) The global Express error handler forwarded `err.message` to the client even for unexpected 500s, which can leak internal details (Postgres error text includes table/column names) — now 5xx always returns a generic message while the real error still goes to the server log (verified against the real backend with a malformed-UUID request). Also added [packages/backend/API.md](packages/backend/API.md), a full endpoint reference, spot-checked against live responses rather than written from memory.

**Honest gaps, not fixed**: no rate limiting or CORS origin restriction yet (both explicitly deferred to Etapa 5 / Dia 75 in the plan); cross-browser testing only covers Chromium (the available tooling drives a Chromium-based browser, not Safari/Firefox).

**Dia 26 is done, starting Etapa 3 (IA & Chat)**. **Project decision (2026-07-23): use the Anthropic API instead of OpenAI** for AI features — the plan's original `OPENAI_API_KEY`/OpenAI SDK references are superseded by `ANTHROPIC_API_KEY`/`@anthropic-ai/sdk` everywhere. `packages/backend/src/services/aiService.js` wraps `client.messages.create` (model `claude-sonnet-5`) with a short system prompt ("professor de inglês técnico"); `POST /api/ai/explain` (`{word, context}` → `{word, explanation}`, auth required) is the first consumer. Since there's no real `ANTHROPIC_API_KEY` yet (the user hasn't finished getting one), the client is constructed with a placeholder key that never crashes app startup, and the controller checks `process.env.ANTHROPIC_API_KEY` up front, returning 503 with a clear message instead of a raw failure — verified for real against the actual unconfigured environment (both a curl call and an integration test assert the 503). `aiService`'s request/response handling is unit-tested with `node:test`'s built-in `mock.method` on `client.messages.create`, so no network call or real key is needed to test it. Not yet done: actually calling the real Anthropic API (needs the user's key) and wiring this endpoint into any UI — that's for a later day once a key exists.

**Dia 27 is done**: `POST /api/ai/chat` (auth required, `{messages: [{role, content}]}` → `{reply}`) was added to the backend first — the plan lists Dia 27 as frontend-only, but a chat UI with nothing real to call would just be a mock, so `aiService.chatReply` + the route were built ahead of schedule (Dia 28 still owns making it context-aware and persisting to `chat_messages`). `ChatWidget.jsx` (mounted in `PlayerPage`, resets per chapter) sends the full client-side message history each turn, shows a typing indicator, and supports copy-to-clipboard and 👍/👎 feedback (feedback is local UI state only, not persisted yet). Verified for real against the live Anthropic API (not mocked) in-browser: registered a user, opened the seeded test audiobook/chapter, and had an actual back-and-forth conversation. Two real bugs were caught this way rather than by inspection: (1) `ChatWidget` and `AudioPlayer` were sibling elements both keyed `chapter.id`, which triggered React's duplicate-key warning — `ChatWidget` now uses `` `chat-${chapter.id}` ``; (2) the feedback buttons used `text-green-600`/`text-red-500` on emoji characters, which has no visual effect since emoji glyphs ignore CSS `color` — switched to a background highlight. A third bug, `bottomRef.current.scrollIntoView is not a function`, only showed up when running the existing Vitest suite (jsdom doesn't implement `scrollIntoView`) — a reminder that manual browser verification and the automated suite catch different things and both are worth running.

**Dia 28 is done, closing Etapa 3's core chat work**: `aiService.buildChatContext(chapterId, wordId)` pulls the chapter's title/transcript and, when a word is in focus, its translation/explanation, and folds it into `chatReply`'s system prompt; `POST /api/ai/chat` now accepts optional `chapterId`/`wordId` and persists every turn (latest user message + reply) to `chat_messages` (`message_type` is `'vocabulary'` when a `wordId` is present, `null` otherwise — no real classifier for grammar/pronunciation exists). `ChatWidget` sends the current `chapterId` (passed down from `PlayerPage`). Verified for real against the live API: the exact question that made the Dia 27 tutor ask "which sentence?" now gets answered by quoting the actual transcript line, and the row lands in `chat_messages` with the correct `chapter_id`. `buildChatContext` is covered by integration tests against real Postgres (chapter-only, chapter+word, missing id, no id at all); `chatReply`'s context-folding is unit-tested. Not done: no endpoint to load past chat history back into the UI on reopen (not asked for by the plan; messages are stored but write-only for now), and `wordId` is accepted by the backend but nothing in the UI sends it yet (no "ask about this word" affordance on `TranscriptDisplay`/`TranslationPopup`).

**Dia 29 is done**: `ChapterFeedback.jsx` shows "Você entendeu este capítulo?" (Sim / Mais ou menos / Não entendi) once `progressByChapter[chapter.id]?.completed` is true; picking "Não entendi" calls the new `POST /api/ai/remedial` (`{chapterId}` → `{summary, keywords, exercise}`, auth required). `aiService.getRemedialContent` asks Claude for a JSON response and falls back to treating the raw text as the summary if parsing fails, so a malformed model response degrades instead of crashing. Route guard order was deliberately chosen as chapterId present (400) → chapter exists (404) → chapter has a transcript (422) → `ANTHROPIC_API_KEY` configured (503), so validation errors aren't masked by the AI-unavailable case in CI (caught by writing the 404/422 integration tests before fixing the order — they failed against the first version). Verified for real: marked a test chapter completed directly in Postgres, clicked all three feedback options in-browser, and got back a coherent AI-generated summary, keyword list, and fill-in-the-blank exercise.

**Dia 30 is done, closing Etapa 3's "Semana 6" block**: `packages/backend/src/services/cacheService.js` wraps the `redis` client (get/set/del, 24h default TTL, `invalidateByPrefix` via SCAN) with best-effort error handling — a Redis failure never breaks a request, it just skips the cache and calls the AI directly. `explainWord` and `remedial` now check the cache first (a hit skips the `ANTHROPIC_API_KEY` requirement entirely) and populate it after a real AI call; cache keys are built by `explainCacheKey`/`remedialCacheKey` in `aiService.js` so the matching `invalidateExplainCache`/`invalidateRemedialCache` (added but not called by anything yet — no content-editing feature exists to trigger them from) can't drift out of sync. Chat isn't cached — conversations are inherently unique per history, unlike explain/remedial which are pure functions of their input. **No Redis available on this dev machine** (no Docker/WSL either, unlike Postgres which had a real local install) — a real `redis:7` service was added to CI's `test.yml` instead, and two integration tests seed a cache entry directly and confirm the endpoints serve it without an API key. A real bug was caught by actually running the tests: the redis client's default reconnect strategy retries forever with no cap, so every cache lookup against an unreachable Redis hung and froze the whole suite — fixed with `reconnectStrategy: false` + a 2s `connectTimeout`.

**Post-Dia-30 incident**: the first push of the Redis work hung CI's `test` job for over an hour (had to be cancelled manually) — `ai.test.js`'s new cache-hit tests open a real connection to the CI Redis service, which keeps the event loop alive, so `node --test` never exits on its own once tests finish (every other test file only mocks the redis client, so this was invisible locally). First fix attempt (`--test-force-exit`) caused a native crash locally instead (libuv `UV_HANDLE_CLOSING` assertion on Windows from force-killing mid-close) - too blunt. Real fix: `config/redis.js` exports `closeRedis()`, called from a file-level `after()` in `ai.test.js` once all its tests finish. Confirmed fixed: the next CI run completed in the normal ~2min instead of hanging.

**Dia 31 is done, starting Etapa 3's Semana 7**: `packages/backend/src/services/difficultyService.js` has three query functions built on data already being collected — `getStruggledWords` (words a user clicked more than once, from `word_clicks`), `getLowCompletionChapters` (lowest completion rate **across every user**, a content-level signal rather than a personal one), and `getFrequentQuestionChapters` (chapters a user has asked the AI tutor about most, from `chat_messages` since Dia 28). `GET /api/user/difficulty-profile` (auth required) combines all three into one response. Verified against real Postgres with seeded fixtures for both the "struggling" and "not struggling" cases, plus an end-to-end test through the real HTTP server. Also caught up `API.md`, which had silently drifted out of date since Dia 25/26 (missing `/api/ai/chat`, `/api/ai/remedial`, and the Redis caching behavior entirely) — spot-checked against the current controllers rather than written from memory.

Not done: nothing consumes this endpoint in the UI yet (no dashboard widget surfaces it) - that's implicitly Dia 32's job (intelligent repetition scheduling using this same difficulty data), not asked for by Dia 31's plan.

**Dia 32 is done**: `wordRepetitionService.findRepeatedDifficultWords(userId, chapterId)` matches a chapter's words by text (case-insensitive) against every `word_clicks` row the user has on that same text anywhere, surfacing words they've struggled with that resurface in that chapter — `GET /api/user/chapters/:chapterId/repeated-words`. `schedulingService.getStudyPriority(userId)` re-ranks the user's non-mastered flashcards by `word_clicks` count first, then by `next_review` — a deliberate departure from Dia 17's plain SM-2 ordering, since a word clicked 4 times but not due for 10 days still needs more attention than one clicked once and due today — `GET /api/user/study-priority`. Both verified against real Postgres, including a case-insensitive text match test for the repetition finder. Caught a real bug while writing the scheduling test fixtures: an unused bound parameter in a raw `INSERT` (passed in the values array but never referenced by a `$n` placeholder in the query text) made Postgres unable to infer its type — same class of issue as the Dia 17 `make_interval` bug, different cause. Neither endpoint is consumed by the UI yet (no "words to watch for" banner on the player, no dedicated priority-queue view) — that UI work isn't explicitly asked for by Dia 32's plan either.

**Dia 33 was done, closing Etapa 3's Semana 7, then removed on 2026-07-24**: originally added Deepgram-based server-side pronunciation scoring (`POST /api/pronunciation/score`, `services/deepgramService.js`, a second "Analisar com IA" button in `PronunciationRecorder.jsx`). The user reviewed it and asked to eliminate it as unnecessary, so it was fully reverted: the backend route/controller/services and their tests were deleted, `@deepgram/sdk`/`multer` uninstalled, `DEEPGRAM_API_KEY` removed from `.env`/`.env.example`, and `PronunciationRecorder.jsx` reverted to its Dia 18 Web Speech API-only form. `AI_GUIDELINES.md`/`API.md` updated to match. Nothing Deepgram-related remains in the codebase.

**Dia 34 is done, starting Etapa 3's Semana 8**: `packages/backend/src/services/recommendationService.js` is deliberately rule-based, not an LLM call — deterministic and directly testable, unlike an AI-generated suggestion would be. `getNextChapterRecommendation` (continue where the user left off: lowest incomplete chapter in their most-recently-accessed audiobook), `getRecommendedAudiobook` (an unstarted audiobook, preferring the category/level they already engage with), and `getBestStudyHour` (hour of day with the most `word_clicks` activity, server timezone). `GET /api/user/recommendations` combines all three; `RecommendationsFeed.jsx` renders up to 3 cards on the dashboard, failing silently (renders nothing) since it's non-critical. Verified end-to-end against real data: correctly recommended the one audiobook the test user hadn't started, and correctly hid the other two cards (finished their only chapter, no click history). Caught a real Postgres bug via the tests: `ORDER BY (boolean expression) DESC` put `NULL`-condition rows (audiobooks with no category/level set) ahead of the actual `true` match, because Postgres defaults to `NULLS FIRST` for `DESC` — fixed with `COALESCE(..., false)`.

**Dia 35 is done**: `packages/backend/tests/quality/` holds live-API tests against the real Anthropic API (not mocked) — `aiResponseQuality.js` asserts structural properties (mentions the right word, avoids markdown, addresses the question, well-formed JSON) rather than exact text, since LLM output isn't deterministic; `aiLatency.js` benchmarks and logs real timing. Both are deliberately named without `.test.js` so the default `npm test` (which auto-discovers that pattern) never runs them — they only run via the new `npm run test:ai-quality` script, and skip cleanly (not fail) without `ANTHROPIC_API_KEY`. Real measured numbers (single-run, this machine): `explain` ~2.2-2.6s, `chat` ~5.4-6.2s, `remedial` ~4.5-5.1s — `chat` is slowest since it's the only uncached endpoint and can carry full conversation history. `AI_GUIDELINES.md` documents model choice, these latency numbers, the cost model (pay-as-you-go, no tracking/rate-limiting yet — Dia 37's job), prompt-engineering conventions used so far, and a checklist for adding new AI features.

**Dia 36 is done**: `POST /api/voice/command` (auth, `{transcript, context?}`) parses a fixed MVP command set with regex, not an LLM — "explain X"/"what does X mean" (reuses `explainTechnicalTerm`), "next chapter"/"play next chapter" (pure intent, frontend navigates), "check my progress"/"my progress" (reuses stats), anything else → `unknown`. Refactored `statsController`'s inline queries into `statsService.getUserStats(userId)` so the voice "progress" intent and the existing `GET /api/user/stats` share one implementation. `VoiceCommandBar.jsx` (mounted in `PlayerPage`) uses the Web Speech API to capture a command and act on the response, reusing the same next-chapter handler as the existing button. Verified: `getUserStats` refactor confirmed working end-to-end (real stats returned via the voice endpoint), and in-browser the button/hint render correctly with a denied mic permission caught gracefully — same sandboxed-preview limitation as `PronunciationRecorder`, so the recognized-speech path is untested end-to-end.

**Dia 37 is done**. Four cost levers, all real and verified against the live Anthropic API (not mocked): (1) **model tiering** — `explainTechnicalTerm` now runs on `claude-haiku-4-5` instead of Sonnet (short, high-volume, cacheable — the exact candidate flagged back on Dia 35/36), while `chat`/`remedial` stay on `claude-sonnet-5` since they need more reasoning; (2) **per-user rate limiting** — `services/rateLimitService.js` (Redis fixed-window counter, fails open on a Redis outage) + `middleware/rateLimiter.js` cap every `/api/ai/*` route at `AI_DAILY_RATE_LIMIT` requests/user/day (default 50); the voice assistant's `explain` intent checks the same limiter inline (not blanket-applied to the whole `/api/voice` router, since `next_chapter`/`progress` don't call AI and shouldn't burn a user's quota) — a 429 is returned before the `ANTHROPIC_API_KEY`/cache checks even run; (3) **cost tracking** — `services/costTrackingService.js` logs every real (non-cached) call to a new `ai_usage_log` table (migration 007: `user_id`, `endpoint`, `model`, `input_tokens`/`output_tokens` from the Anthropic response's real `usage` field, `estimated_cost_usd` from a hardcoded per-model price table), best-effort so a logging failure never breaks the actual AI response; (4) **prompt engineering** — every system prompt now explicitly forbids greetings/preambles, cutting wasted output tokens. Also fixed a related gap found while verifying rate-limit headers in-browser: `cors()` had no `exposedHeaders`, so `X-RateLimit-*` was on the wire but invisible to frontend JS — added `exposedHeaders: ['X-RateLimit-Limit', 'X-RateLimit-Remaining']`. Verified for real: hit `/api/ai/explain` live and confirmed (a) the response came from Haiku, (b) a correct row landed in `ai_usage_log` with real token counts and cost, (c) the chat endpoint still works end-to-end with the tightened prompt. The two new rate-limit integration tests correctly fail on this machine (no local Redis → fails open, exactly as designed) and are expected to pass in CI, which has a real Redis service — same accepted local/CI gap established on Dia 30.

Not done: no cost dashboard consuming `ai_usage_log` yet (nothing asked for one); `AI_DAILY_RATE_LIMIT`'s default of 50/day is a starting guess, not tuned against real usage data (there isn't any yet).

**Dia 38 is done**: tracks the four signals the plan asked for — questions per chapter, response time, user satisfaction, cost per user — all real, all verified live. `migration 008` adds `users.is_admin` (boolean, default false, no self-service way to become one — set directly in the DB), `chat_messages.feedback` (`'helpful'`/`'not_helpful'`/`null`), and `ai_usage_log.response_time_ms`. `aiService.js`'s `createMessage` wrapper now times every `client.messages.create` call and threads `responseTimeMs` through to `costTrackingService`. `POST /api/ai/chat` now returns `messageId`, and a new `PATCH /api/ai/chat/:messageId/feedback` persists the 👍/👎 `ChatWidget` already had wired to local-only state since Dia 27 — that gap is closed. `services/analyticsService.js` has four query functions (global, not per-user — a deliberate difference from Dia 31's `difficultyService`), combined by `GET /api/admin/analytics` behind a new `requireAdmin` middleware. `AdminAnalyticsPage.jsx` (`/admin/analytics`, nav link only rendered for `user.isAdmin`) renders it as tables; `ProtectedRoute` gained a `requireAdmin` prop. Verified for real: flagged the existing `chattest@techspeaking.dev` test user as `is_admin` directly in Postgres, logged in fresh (the JWT/user object doesn't refresh admin status without a new login), confirmed the Analytics nav link and page render with genuine data, asked the chat tutor a real question, clicked 👍, watched it PATCH successfully, confirmed the row in `chat_messages` in Postgres, and confirmed the analytics page's satisfaction/cost/question-count numbers updated correctly on refresh.

Not done: no way to promote a user to admin from the UI (matches the plan's own scope — it doesn't ask for user management here); the response-time average is a global aggregate that only includes calls made since Dia 38's migration (pre-existing `ai_usage_log` rows have `NULL` and are correctly excluded, not counted as 0-latency).

**Dia 39 is done**: `services/aiFallbackService.js` provides graceful degradation for a genuine runtime AI failure (Anthropic outage, rate limit, network error/timeout) — deliberately distinct from the existing "`ANTHROPIC_API_KEY` not configured" 503, which is a deployment/config signal, not a failure to recover from, and keeps its existing (extensively tested) contract unchanged. `explain` (both `/api/ai/explain` and the voice assistant's `explain` intent) falls back to a real lookup in `technical_dictionary` (Dia 16) when one exists, or a generic FAQ otherwise; `chat`/`remedial` fall back to a small static FAQ + an external docs link (MDN) + a support email (`suporte@techspeaking.dev`) — all still returned as **200**, not an error status, since the app is providing a genuinely useful degraded response, not failing outright. A fallback chat reply is deliberately **not** persisted to `chat_messages` (it isn't a real tutor turn and would pollute Dia 38's analytics). `ChatWidget` renders fallback replies in a distinct amber bubble with the FAQ/docs/support below (and hides the now-meaningless copy/👍/👎 controls); `ChapterFeedback`'s remedial panel does the same. Verified for real, twice: integration tests mock `client.messages.create` to throw (proving the code path), and separately — since this dev machine has a real `ANTHROPIC_API_KEY` — the real key was temporarily swapped for an invalid one, the backend restarted, and both the chat and remedial fallback UIs were exercised live against a genuine Anthropic 401, then the real key was restored and a normal chat reply was confirmed working again before moving on.

**Dia 40 is done, closing Etapa 3 (Semanas 6-8, Dias 26-40) for real**. Small, real refactor: the `ANTHROPIC_API_KEY`-missing check was duplicated verbatim 4 times (`aiController.js` ×3, `voiceController.js` ×1) — extracted to `aiService.isAiConfigured()` + `aiService.AI_NOT_CONFIGURED_ERROR`; the daily-rate-limit env parsing was duplicated in `rateLimiter.js` and `voiceController.js` — extracted to `rateLimitService.getDailyAiLimit()`. Both are literal-duplication removals, not new abstraction — behavior and response text are byte-identical, confirmed by the full test suite staying green with no new failures. The rest of the day is the plan's actual ask: reviewing and honestly documenting where Etapa 3 (Dias 26-39) actually stands, in `AI_GUIDELINES.md`'s new "Known limitations" and "Review checklist" sections — no streaming (chat's ~6s wait has no partial output), no retry-on-failure before falling back, an approximate/hardcoded cost model, an untuned rate-limit default, non-context-aware fallback content for chat/remedial, no cross-session chat memory, and Portuguese-only content. The plan's four checklist items were answered honestly rather than assumed: re-ran `npm run test:ai-quality` live (6/6 passing, latency table refreshed — `explain` is now measurably faster since Dia 37's Haiku switch, ~1.2-2.5s vs ~2.2-2.6s before) and queried the real `ai_usage_log` for actual spend (16 calls, $0.0146 total) to answer "funciona/performance/custo" with real numbers instead of assumptions; "usuários acham útil" is flagged as genuinely unverifiable — there's no real user base to ask.

**Dia 41 is done, starting Etapa 4 (Conteúdo & Monetização, Semanas 9-12)**. Content-only day, no application code: `packages/backend/content/AUDIOBOOK_TEMPLATE.md` documents the standard 5-chapter audiobook structure (Opening/Updates/Blockers/Planning/Closing, mirroring a real daily standup) with exact field-to-column mappings against the real schema, and uses the existing seeded chapter (`11111111-1111-1111-1111-111111111111`, "Opening": "Yesterday I deployed a new version.") as the worked example — it's genuinely chapter 1 of this template, not an invented sample. `packages/backend/CONTENT_GUIDELINES.md` explains the *why* (the standup format teaches workplace communication pattern alongside vocabulary) and the constraints future content must satisfy to work with already-built features: plain-text transcripts (read directly by `chat`/`remedial`'s AI prompts), one coherent scene per chapter, natural spoken sentences (scored verbatim by `PronunciationRecorder`), and a "check `technical_dictionary` before inventing a new word" rule so vocabulary compounds across a series via spaced repetition instead of resetting per audiobook. Also cleaned up an orphaned `Test Audiobook` row found in the dev DB (leftover from an interrupted local test run, not committed anywhere) — unrelated to today's work, just DB hygiene.

Not done: no new audiobooks were actually written today — chapters 2-5 of the existing "Daily Standup" audiobook, plus a full first series, is explicitly Dia 42's job per the plan, building on today's template rather than overlapping it.

**Dia 42 is done**: wrote the plan's 5 initial audiobooks (Daily Standup, Git & Code Review, API Design, Cloud & DevOps, Interview Preparation — 25 chapters total), following Dia 41's template. Migrations 009-013, one per audiobook, each `ON CONFLICT DO NOTHING`/idempotent so they're safe to re-run. Migration 009 also **backfills the pre-existing "Daily Standup"/"Opening" seed data into version control for the first time** — it turned out that fixture (used throughout this project's manual verification steps since Dia 11) had only ever been manually inserted into this local dev Postgres, never committed; a fresh environment (including CI) never actually had it. `technical_dictionary` grew from 24 to 30 entries (6 new: `priorities`, `reviewed`, `instance`, `architecture`, `trade-off`, `complexity`), added ahead of the content that introduces them, per Dia 41's "reuse before inventing" rule.

Writing this content surfaced a real, pre-existing bug in `TranscriptDisplay.jsx`: it rendered the transcript by concatenating only the *tagged* words, so any chapter tagging just a few technical terms (not literally every word, as the original single test chapter happened to do) would silently drop the rest of the sentence from view. Fixed by tokenizing the real `transcript` string and matching tagged words (including multi-word terms like "pull request") against it — untagged tokens now render as plain text instead of vanishing, so partial vocabulary tagging (the model this project's own `CONTENT_GUIDELINES.md` describes) actually works. Caught and fixed a second issue while re-verifying: `PlayerPage.test.jsx` used `findByText('deployed')` to locate the clickable word, but before the async words-fetch resolves, "deployed" briefly exists as plain (non-clickable) text too — `findByText` could grab that transient node, then click a since-detached element. Fixed by querying `findByRole('button', { name: 'deployed' })` instead, the correct semantic query for an interactive element. No real audio exists for the 24 new chapters (`audio_url`/word timestamps are `NULL`) since TTS/audio production isn't built yet (tracked gap, architecture section) — click-to-translate, chat, remedial, and pronunciation practice all work regardless, since none of them depend on `audio_url`.

Verified for real: all 5 audiobooks appear in `/audiobooks`; the new partially-tagged content (e.g. "branch" as the only clickable word in a full sentence) renders and translates correctly via the `technical_dictionary` fallback; the original fully-tagged "Opening" chapter still renders identically (regression check); a new dictionary term (`priorities`) resolves correctly through a newly-written chapter. Full backend (142 tests) and web (21 tests, up from 19) suites pass.

**Dia 43 is done, with two deliberate scope decisions**: `POST /api/admin/audiobooks` (auth + `is_admin`, reusing Dia 38's `requireAdmin`) accepts a `multipart/form-data` upload (`multer`, memory storage) — title/transcript/description/category/level/chapterTitle plus an audio file and optional `words_metadata` JSON — uploads the file to AWS S3 (`@aws-sdk/client-s3`, `services/audioStorageService.js`) and creates the audiobook + one chapter + tagged words in a single transaction. Two things the plan asked for were intentionally **not** built: (1) **Deepgram auto-timestamp generation** — Deepgram was removed on Dia 33 at the user's explicit request ("acho desnecessário"); reintroducing it now for this feature would reverse that decision, so timestamps are supplied directly via `words_metadata` instead (a real, if more manual, path — same shape the plan's own schema already listed); (2) **real audio transcoding** — there's no ffmpeg pipeline in this project and no real audio content exists yet to transcode, so "processing" is an MVP format allowlist (`isAcceptedAudioType`) rather than actual conversion. Both are documented as honest gaps, not silent omissions.

Like `ANTHROPIC_API_KEY` before Dia 26, this dev environment has no real AWS credentials, so `isS3Configured()` gates the endpoint with a 503 exactly like `isAiConfigured()` does — verified for real (genuine 503, no key configured) via curl against the live dev server, along with the 400 validation paths (missing title, missing file, unsupported format) and the 401/403 auth gates. The full success path (S3 upload → DB writes) is verified via an integration test that mocks `s3Client.send` (same established pattern as Dia 39's AI-fallback tests) — asserts the audiobook/chapter/words rows land correctly and the returned `audioUrl` has the right shape; a genuine end-to-end S3 upload remains unverified pending real credentials.

**Dia 44 is done**: the plan's "Admin Panel" (5 sections) is real where the app has real data behind it, and honestly stubbed where it doesn't. New `/admin` hub (`AdminLayout.jsx`, tab nav) wraps 4 pages: **Upload Audiobooks** (`AdminUploadPage.jsx`, a real form wired to Dia 43's `POST /api/admin/audiobooks` — the `words_metadata` field is a raw JSON textarea, an honest MVP given there's no visual timestamp editor); **Analytics** (Dia 38's existing page, just linked in, not rebuilt — it already covers "Monitor IA Costs" via its cost-per-user table); **Usuários** (new `GET /api/admin/users` + `PATCH /api/admin/users/:id`, `services/adminUserService.js`) — the one concrete "gerenciar usuários" action built is promoting/demoting admins, since that's the specific documented gap from Dia 38 ("no way to promote a user to admin from the UI"); a self-demotion guard rejects an admin trying to remove their own access, since `is_admin` has no self-service path back in. **Revenue** is a deliberate honest stub: there's no billing/Stripe integration yet (that's Dia 46+ in the plan), so rather than fabricate revenue numbers, the page states that plainly and shows the one genuine related data point available — user counts by `plan` (all "free" today, since no paid tier exists).

Verified live end-to-end: submitted the real upload form (with a JS-injected file, since this sandboxed browser can't drive a native file picker) and confirmed the frontend correctly surfaces the real 503 from Dia 43's AWS-not-configured gate; promoted and reverted a real user's admin status via the Users page, confirming the table refreshes and the self-demotion button is genuinely disabled for the logged-in admin's own row; confirmed Revenue renders real plan-distribution data; confirmed Analytics still renders correctly through the new shared layout (no regression). Also cleaned up 23 orphaned `test-*@techspeak.test` rows in the dev DB (visible for the first time in the new Users page) — leftovers from earlier interrupted local test runs this session, not a code issue.

**Dia 45 was explicitly skipped, per user decision (2026-07-25)**: the plan's "Sistema de Transcrição Automática" asks to reintegrate Deepgram (this time for auto-transcribing uploaded audio, not the pronunciation-scoring use case removed on Dia 33). Asked the user first, since it directly intersected that prior decision; the user doesn't want to pay for Deepgram or any equivalent paid transcription API. Content authoring stays fully manual (hand-written transcripts + manually specified `words_metadata` timestamps, same as Dia 41-43) — a real cost to authoring speed, not a gap in what the product does for learners. Revisit only if manual timestamping becomes a genuine bottleneck at higher content volume; a self-hosted Whisper model was named as the one realistic free alternative if that day comes, but it's real infra work, not justified yet.

**Dia 46 is done**: `PRICING.md` (repo root, alongside the existing business positioning notes) documents the plan's 3-tier structure (Free/Pro/Corporate) with concrete limits, grounded in what's real in this product today rather than generic SaaS boilerplate — e.g. "2 audiobooks" is sized against the actual 5-audiobook catalog (Dia 42), "unlimited AI" against the real `AI_DAILY_RATE_LIMIT` mechanism (Dia 37). `packages/backend/MONETIZATION.md` is the technical companion: a feature-by-feature table mapping each tier's promise to the exact file/function that would enforce it, and honestly marking every single one as **not currently enforced** — `users.plan` has existed since Dia 1-2 and is already returned to the frontend, but nothing in the codebase reads it to gate anything yet. Docs-only day, no application code changed.

**Pricing model finalized (2026-07-25), superseding Dia 46's subscription draft**: after discussing tradeoffs, the user chose lifetime access over a monthly/annual subscription — R$ 57 one-time (`TechSpeak Vitalício`), positioned as an accessible Brazilian infoproduto price point rather than a premium SaaS subscription, explicitly optimizing for volume over margin-per-user. `PRICING.md` and `MONETIZATION.md` were rewritten to match: content (audiobooks, flashcards, translation, pronunciation) is unlimited under this model since it's free/near-free to serve, while the AI chat tutor keeps a flat 10-messages/day cap **even for paying users** — a deliberate departure from the usual "paid = unlimited" SaaS gate, needed because a one-time payment against an unbounded ongoing AI cost (Dia 37: ~R$0,002-0,02/call) is a real risk over a long enough usage horizon. Future audiobooks beyond the initial catalog are planned as separate one-time add-on purchases (R$19-29 each), which is also what motivated adding 2 more audiobooks now (see below) — enough catalog depth to make the initial R$57 clearly worth it before that add-on mechanism exists.

Added **2 new audiobooks** (**Incident Response**, DevOps/intermediate, and **Remote Work Communication**, Career/beginner — 5 chapters/10 words each, migrations 014-015) to strengthen the catalog ahead of the pricing decision, following Dia 41's template exactly. 8 new `technical_dictionary` terms (`root cause`, `mitigate`, `escalated`, `postmortem`, `async`, `time zone`, `concise`, `visible`), bringing the dictionary to 38 entries and the catalog to 7 audiobooks / 35 chapters total. Verified live: both audiobooks browse and play correctly, the new multi-word dictionary term `root cause` resolves correctly through the same `technical_dictionary` fallback (translation, explanation, context tags all correct) — confirming Dia 42's content-authoring pattern and Dia 42's `TranscriptDisplay` partial-tagging fix both continue to hold up for freshly-written content.

**Catalog expanded to 25 audiobooks (2026-07-25), ahead of Dia 47**: per the user's request to build out the library before launch (so the R$57 vitalício price feels justified by real content depth, not just technology), added 18 more audiobooks (migrations 016-033) on top of the existing 7, following Dia 41's template exactly — 125 chapters total, ~7 hours of planned content (`duration_minutes` sums to 426), 88 `technical_dictionary` entries (up from 38). New topics span both technical scenarios (Sprint Planning & Retrospectives, Code Review Etiquette, Database & Performance, Testing & QA, Security Basics, Technical Writing, Onboarding, Open Source Contribution, Debugging in Production, Architecture Decisions, CI/CD Troubleshooting) and career/soft-skill scenarios directly relevant to the product's audience (1:1s & Performance Reviews, Negotiating a Job Offer, Presenting to Stakeholders, Freelancing & Contracts, Networking at Tech Events, Building a Promotion Case, Customer Support Escalation) — deliberately not just more DevOps content, since the product's positioning already covers career advancement, not only technical vocabulary.

Writing this much content at once surfaced a real, systemic content bug worth calling out: **word-form mismatches**. `TranscriptDisplay` (Dia 42's fix) matches a tagged `words.word` against the transcript by exact normalized text — it does *not* stem or lemmatize, so tagging `'merge'` when the sentence says "I **merged** the branch" silently fails to match, and the word is never clickable, with no error anywhere to surface it. A verification script (querying every `words` row, tokenizing its chapter's transcript, and checking the tagged word actually appears) caught **4 real instances**: 2 pre-existing from Dia 42 itself (`merge`/`merged` in Git & Code Review, `deprecate`/`deprecated` in API Design — fixed via a new migration 034, since those were already committed/pushed history) and 2 in this batch's own new content (fixed by editing the not-yet-applied migration files directly before commit). This verification script is a genuinely useful tool this project didn't have before — worth remembering as a pre-commit check for any future content batch, since manual browser spot-checking alone would not have caught all 4 (only checks a handful of chapters, not all 127 tagged words).

Verified for real: all 127 tagged words across the full catalog programmatically confirmed to match their transcripts and resolve via `technical_dictionary` (no orphans); live-checked a new multi-word term (`elevator pitch`) end-to-end in-browser. Full backend suite (165 tests) stays at the same known-failure baseline (8, all pre-existing environment gaps) — no regressions from the content batch itself.

**Dia 47 is done**: `POST /api/payment/create-checkout-session` (auth required) creates a one-time Stripe Checkout Session for the R$ 57 `TechSpeak Vitalício` — deliberately `mode: 'payment'` with an inline `price_data` line item (no pre-created Stripe Price ID needed, one less manual dashboard step), not the plan's original `create-subscription` shape, matching Dia 46's lifetime-purchase decision. `POST /api/payment/webhook` verifies the `Stripe-Signature` header and sets `users.plan = 'pro'` on `checkout.session.completed`; every other event type is acknowledged without action. The webhook route is registered in `app.js` with `express.raw()` *before* the global `express.json()` middleware, since Stripe's signature verification needs the exact raw bytes it signed — parsing it as JSON first would break verification for every real webhook call.

Same graceful-degradation pattern as every prior external integration: `isStripeConfigured()`/`isWebhookConfigured()` gate both routes with a 503, and this dev environment genuinely has no Stripe account yet (user confirmed no keys to add for now, explicitly asked whether adding real keys later — after the project is otherwise finished — would require any code changes; answer: no, just setting the env vars). Verified for real via curl against the live dev server: genuine 503 on both routes (no keys configured), 401 on the unauthenticated checkout path. The full success paths (session creation, webhook signature verification, `plan` update) are covered by tests that mock the Stripe SDK's `checkout.sessions.create`/`webhooks.constructEvent` (same established pattern as Dia 39/43's mocked-external-API tests) — a genuine end-to-end Stripe test-mode flow remains unverified pending real keys.

**Dia 48 is done, closing the plan's Checkout Page work**: `GET /api/auth/me` (auth required) reads the caller's row fresh from Postgres, added specifically to fix a real staleness gap — `user.plan` otherwise only changes on next login, since the JWT payload isn't reissued when Dia 47's webhook flips `plan` to `'pro'` mid-session (the same class of staleness already documented for `is_admin` since Dia 38). `AuthContext.refreshUser()` calls it and merges the fresh `user` back into the persisted auth state. `PricingPage.jsx` (`/pricing`, public route) shows the R$ 57 feature list and a "Comprar Vitalício" button: redirects to `/login` if unauthenticated, calls `POST /api/payment/create-checkout-session` and forwards the browser to the returned Stripe URL if authenticated, and renders a real error message (including Dia 47's 503 "not configured" text) inline on failure instead of the button silently doing nothing; a user whose `plan` is already `'pro'` sees an owned-state message instead of the button. `PaymentSuccessPage.jsx` (`/payment/success`, protected route, matches `paymentService.js`'s hardcoded `success_url`) polls `refreshUser()` up to 5 times (2s apart) since the Stripe redirect can beat the webhook back to the app, then shows confirmation once `plan` flips or a "still confirming, check back / email support" message if it doesn't catch up in time. `PaymentCancelPage.jsx` (`/payment/cancel`) is a plain cancellation notice linking back to `/pricing`. `Navbar.jsx` gained a plan-status badge (green "Vitalício" vs. an amber "Upgrade" link to `/pricing`), the plan's third Dia 48 commit.

Verified for real end-to-end in-browser (dev server + real local Postgres, no Stripe keys): registered a fresh test user, confirmed the not-configured 503 renders correctly on `/pricing` (proving the error path works, not just the happy path — there are no real Stripe keys to test the happy path against yet), then directly flipped the test user's `plan` to `'pro'` in Postgres (simulating what the webhook does) and confirmed `/payment/success` picked it up via the polling `refreshUser()` call, the Navbar badge updated to "Vitalício", and `/pricing` correctly switched to the owned-state message — the exact "Ativa Pro features" step from the plan's flow diagram, working without a re-login. Full web suite (21 tests) and lint stay green, no regressions. Test user cleaned up from the dev DB afterward.

Not done: no real Stripe test-mode checkout was exercised (still no account/keys — same accepted gap as Dia 47), and success/cancel URLs are English-passthrough backend error text on the 503 path rather than translated, consistent with how every other external-service 503 message already behaves in this app (untranslated is the existing convention, not a new gap).

**Dia 49 is done**: the actual paywall enforcement `MONETIZATION.md` had been listing as gaps since Dia 46. Migration 035 adds `audiobooks.is_free` (boolean) and marks exactly 2 audiobooks as free — "Daily Standup" (the original flagship technical content) and "Remote Work Communication" (career/soft-skill) — deliberately one from each of the catalog's two content pillars, both beginner level, matching `PRICING.md`'s "Free — 2 audiobooks" trial. `GET /api/audiobooks/:id/chapters` (the endpoint that actually serves chapter transcripts) now 403s with a clear upgrade message for non-free audiobooks unless the caller's `plan` is `pro`/`corporate`; browsing (`GET /api/audiobooks`, `GET /:id`) and `GET /chapters/:chapterId/words` stay fully public/ungated, since only the transcript itself is the real paid content and translation lookups cost nothing regardless of plan. A new `optionalAuth` middleware (populates `req.user` from a valid bearer token but never rejects) keeps the free audiobooks playable by anonymous visitors while still letting the controller check plan for gated ones. `services/planService.js` (`getUserPlan`/`isPaidPlan`) does a fresh DB read per check, deliberately following the same pattern as Dia 38's `requireAdmin.js` rather than embedding `plan` in the JWT — the access token still only carries `sub`/`email`.

The AI rate limiter (Dia 37) is now plan-aware too: `rateLimitService.getDailyAiLimit(plan)` returns `AI_DAILY_RATE_LIMIT_FREE` (default 1/day) for free-plan users and `AI_DAILY_RATE_LIMIT_PRO` (default 10/day) for paid — replacing the old single global `AI_DAILY_RATE_LIMIT` env var. This is deliberately not "unlimited for paying users," per `MONETIZATION.md`'s AI-cost-problem reasoning (a lifetime purchase against unbounded ongoing AI cost is a real risk). `middleware/rateLimiter.js` and `voiceController.js`'s inline `explain` intent both fetch the caller's plan before computing their limit.

Frontend: `PlayerPage.jsx` now sends its access token on the chapters fetch (previously omitted — the endpoint was always public, so this was silently never needed until today) and catches a 403 specifically (`services/api.js`'s `apiRequest` now attaches `err.status` to thrown errors) to render a dedicated "Este audiobook é exclusivo do Vitalício" panel with a link to `/pricing`, instead of the generic red error text. `AudiobookListPage.jsx` shows an amber "Vitalício" badge next to every non-free audiobook when the viewer isn't `plan === 'pro'` (covers both anonymous visitors and logged-in free users) using the `is_free` field `listAudiobooks` now returns.

Verified for real end-to-end in-browser: anonymous view showed the "Vitalício" badge on all 23 gated audiobooks and none on the 2 free ones; a fresh free-plan test user could fully play "Daily Standup" but hit the paywall panel on a gated audiobook; flipping that same user to `plan = 'pro'` directly in Postgres and re-logging-in unlocked the same audiobook and removed all badges from the list. New integration tests (`audiobookPaywall.test.js`, 6 cases) cover anonymous/free/pro × free/paid-audiobook combinations plus the 404 case; `rateLimitService`/`rateLimit.test.js` were updated for the plan-aware signature, including a new case confirming a pro-plan user gets the higher limit instead of the free one. Full backend suite (197 tests, up from 183) stays at the same known baseline of 8 local-only failures (this machine's configured `ANTHROPIC_API_KEY` + no local Redis) — no regressions; web suite (21 tests) and lint stay green.

Not done: the AI rate limit is still one shared bucket across `explain`/`chat`/`remedial` combined rather than chat-specific (matches the existing Dia 37 scope, not a new gap); no UI surfaces "N mensagens restantes hoje" from the `X-RateLimit-Remaining` header in `ChatWidget` (would need `apiRequest` to expose response headers, not just the parsed body — a reasonable future nicety, not asked for by Dia 49's plan).

**Dia 50 is done, closing Etapa 4's Semana 9-10 payment work**: the plan's checklist assumes a subscription (with a cancellation step); adapted for this project's one-time-purchase model — see `packages/backend/PAYMENT_TROUBLESHOOTING.md`'s opening section for the explicit checklist mapping. Three new tests in `payment.test.js`: webhook idempotency (Stripe redelivering the same `checkout.session.completed` event twice doesn't double-charge-equivalent or error — `grantLifetimeAccess` is a plain `UPDATE`, safe to run twice), a missing-`metadata.userId` event doesn't crash the webhook handler (confirms the existing defensive `if (userId)` branch, added back on Dia 47, actually degrades gracefully rather than just looking like it does), and a new "Payment sandbox flow" test chaining `create-checkout-session` → webhook completion → `GET /api/auth/me` in one test to prove the full purchase flow works end to end through our own code — only the Stripe SDK calls themselves are mocked, the only boundary not controllable without real keys.

`PAYMENT_TROUBLESHOOTING.md` (new) is the actual deliverable for whenever real Stripe test-mode keys exist: getting test keys, using the Stripe CLI (`stripe listen --forward-to`) to receive webhooks on localhost, the standard `4242 4242 4242 4242` test card, and a common-failure-modes list grounded in this codebase's real gotchas (the `express.raw()`-before-`express.json()` ordering requirement from Dia 47, checking the CLI's own signing secret vs. a dashboard endpoint's, restarting the backend after editing `.env` since env vars are read once at startup). Verified for real: all 11 payment tests pass locally (200 total backend tests now, same known baseline of 8 local-only failures — no regressions).

Not done: a genuine Stripe test-mode checkout has still never been exercised — this dev environment has no Stripe account (confirmed with the user on 2026-07-25, same accepted gap as Dia 47/48) — so `PAYMENT_TROUBLESHOOTING.md`'s "once you have real keys" section is unverified prep, not a completed checklist item.

**Dia 51-52 is done, starting Etapa 4's Semana 11-12**: `audiobooks.published_at` (migration 036, `TIMESTAMPTZ DEFAULT now()`) turns the existing single-shared-catalog model into a real draft/scheduled/published CMS — `NULL` is a draft, a future date is scheduled, a past/present date is live. The column defaults to `now()` so every pre-existing code path and test fixture that inserts an audiobook without mentioning the column (the entire rest of the codebase, including 6+ other test files' raw SQL fixtures) keeps its prior "published immediately" behavior unchanged; only the new CMS upload path explicitly writes `NULL` to create a draft, which overrides the default since the column is named in that `INSERT`. The public catalog endpoints (`audiobookController.js`: `listAudiobooks`, `getAudiobook`, `getAudiobookChapters`, all from Dia 1/49) now filter to `published_at IS NOT NULL AND published_at <= now()` — a draft or scheduled-for-the-future audiobook 404s exactly as if it didn't exist, same as the Dia 49 paywall's "not found" framing for gated content.

Four new admin-only CMS endpoints in `adminAudiobookController.js`/`adminRoutes.js`: `GET /api/admin/audiobooks` (lists every audiobook regardless of publish state, for the management view), `GET /api/admin/audiobooks/:id` (full detail + chapters, ignoring both publish state and the Dia 49 paywall gate — the actual "Preview" feature), `POST /api/admin/audiobooks/:id/publish` (publishes immediately by default, or schedules a future `publishedAt`), `POST /api/admin/audiobooks/:id/unpublish` (reverts to draft). `POST /api/admin/audiobooks` (Dia 43's upload endpoint) gained an optional `publishedAt` field — a deliberate behavior change from Dia 43: an upload with no `publishedAt` now defaults to a draft instead of going live immediately, so a partially-prepared upload can be reviewed first.

Frontend: `AdminContentPage.jsx` (`/admin/content`, new tab in `AdminLayout`) lists every audiobook with a status badge (Rascunho/Agendado para.../Publicado em...), a Preview toggle that fetches and renders each chapter's transcript read-only, "Publicar agora"/"Despublicar" buttons, and a datetime-local + "Agendar" control for scheduling. `AdminUploadPage.jsx` gained the same datetime-local field, defaulting to blank (draft) with an inline hint, and its success message now says whether the result is a draft or published/scheduled with a link to the new Conteúdo tab.

Verified for real end-to-end in-browser: registered a fresh admin test user, confirmed all 25 existing audiobooks show as "Publicado" with their correct original dates (migration backfill worked), previewed "Building a Promotion Case" and saw all 5 real chapters/transcripts, clicked "Despublicar" and confirmed it flipped to "Rascunho" **and disappeared from the public `/audiobooks` catalog** (24 shown instead of 25), then "Publicar agora" to restore it. Also re-verified the upload form's existing 503 "AWS S3 not configured" path still works correctly with the new field wired in (same accepted gap as Dia 43/44 — no real AWS credentials on this machine). 18 new/updated backend integration tests (`adminAudiobook.test.js`) cover the CMS endpoints, the draft-by-default upload behavior, and a nonexistent-audiobook 404 case; full backend suite (210 tests, up from 200) stays at the same known baseline of 8 local-only failures; web suite (21 tests) and lint stay green.

Not done: no scheduled-publish background job — a "scheduled" audiobook only actually becomes visible the moment someone queries the public endpoints after its `published_at` passes (a lazy/on-read check, not a cron flipping a status flag), which is correct and sufficient here since there's no separate "notify on publish" feature that would need an active trigger. "Agendamento de lançamento" for existing (not-yet-committed) migration-seeded content wasn't touched — this CMS is for the admin-upload path (Dia 43+), not a retroactive scheduling tool for the hand-written catalog.

**Dia 53-54 is done**: `services/contentAnalyticsService.js` + `GET /api/admin/content-analytics` answer the plan's three questions with real queries against existing data, adapted honestly where the plan assumed a subscription model. `getAudiobookCompletionRates()` counts distinct users with any `user_progress` row for one of an audiobook's chapters vs. distinct users with `completed = true` for **every** one of that audiobook's chapters. `getUserRetention()` reinterprets "churn" — TechSpeak has no subscription to cancel (Dia 46's lifetime-purchase decision), so this measures **activity churn** instead: distinct `word_clicks` users in the last 30 days vs. the 30-60-days-ago window, and how many carried over. `getLifetimeValue()` keeps LTV deliberately simple under a one-time-purchase model — `totalRevenueBrlCents` is just `payingUsers × LIFETIME_PRICE_BRL_CENTS` (imported from `paymentService.js`, Dia 47, not duplicated), no recurring-revenue formula since there's no recurring revenue to model. `AdminMetricsPage.jsx` (`/admin/metrics`, new "Métricas" tab) renders all three as stat cards plus a per-audiobook completion table, with the subscription-vs-one-time adaptation spelled out in the page copy itself, not just in docs.

A real testing lesson from this day: the first version of the integration tests asserted exact/`>=` deltas against a `before`/`after` snapshot of global counts (`users`, `word_clicks`) — this flaked under `node --test`'s concurrent file execution, since nearly every other integration test file in this suite creates **and deletes** its own users concurrently against the same real Postgres, so a snapshot-delta comparison isn't safe for tables that busy (unlike Dia 38's `ai_usage_log`/`chat_messages`, touched by only a handful of AI-specific files). Caught by running the full suite 5+ times in a row rather than once (single runs looked green). Fixed by testing the exact arithmetic with mocked data in `contentAnalyticsService.test.js` (deterministic, already reliable) and switching the integration tests to invariants that hold regardless of concurrent state (e.g. `retainedUsers <= previousPeriodActiveUsers`, `totalRevenueBrlCents === payingUsers × LIFETIME_PRICE_BRL_CENTS`) plus floor checks on our own just-inserted fixtures rather than snapshot deltas. Confirmed fixed: 5 consecutive full-suite runs all landed at the same known baseline of 8 failures.

Verified for real against this dev DB's actual accumulated data (52 users, mostly test fixtures from manual verification sessions across prior days): the metrics page correctly showed 1 paying user, R$57 total revenue, R$1.10 average LTV (57/52, arithmetically correct), and real non-zero "started" counts for the audiobooks actually exercised in manual testing (Daily Standup, Git & Code Review, Incident Response, Networking at Tech Events) with 0% completion (accurately reflecting that manual test sessions start chapters but rarely finish a whole 5-chapter audiobook). Confirmed no regression on the existing Dia 38 AI Analytics page. Full backend suite (221 tests, up from 210) stays at the same known baseline of 8 local-only failures across 5 repeated runs; web suite (21 tests) and lint stay green.

Not done: no cleanup of the ~50 leftover test-fixture user rows in this dev DB, so the real conversion-rate/LTV numbers shown are diluted by non-real accounts — an honest caveat on today's verification, not a code gap (matches this project's repeated "no real user base yet" caveat from Dia 35/40).

**Dia 55-56 is done**: the plan's own test list ("Preço Pro: \$9.99 vs \$12.99", "Copy de desconto", "UI do paywall") is written for a USD subscription model that doesn't exist here — adapted to two real experiments grounded in this project's actual pricing/paywall. `services/experimentService.js` defines both in code (not DB-driven): `pricing_price` (`control` R$57 vs `discount` R$47, testing the exact anchors `PRICING.md` itself names as validation candidates) and `paywall_message` (two copy variants for the Dia 49 paywall panel). Variant assignment is a deterministic SHA-256 hash of `experimentName:subjectId` mod variant count — no DB read needed to know a subject's variant, and the same subject always gets the same one. `experiment_events` (migration 037) logs what actually happened: an `exposure` (deduped per subject+experiment via a partial unique index, so repeat page views don't inflate the denominator) or a `conversion`.

`GET /api/experiments/:name/assignment` (public — an anonymous pricing-page visitor needs this before ever logging in) returns the variant + its config and logs the exposure. For the price experiment specifically, real-money conversion is attributed server-side, not client-reported: `createCheckoutSession` computes the variant from a `subjectId` in the request body (the frontend's persisted anonymous visitor id, so the price shown pre-login matches what's charged), stashes `experimentName`/`experimentSubjectId`/`experimentVariant` in the Stripe session metadata (`paymentService.createLifetimeCheckoutSession` gained an options param for this), and the Dia 47 webhook logs the conversion from that metadata when `checkout.session.completed` fires — an actual purchase, not a page click, is what counts as converting. The `paywall_message` experiment's goal (clicking "Ver planos") is lighter-weight and UI-driven, so it uses a generic `POST /api/experiments/:name/conversion` the frontend calls directly. `GET /api/admin/experiments`/`GET /api/admin/experiments/:name/results` (admin-only) expose per-variant exposures/conversions/rate; `AdminExperimentsPage.jsx` (`/admin/experiments`, new "Experimentos" tab) renders them. `PricingPage.jsx` shows the assigned price + discount badge; `PlayerPage.jsx`'s paywall panel shows the assigned message and fires the conversion event on click. `services/experiments.js` (frontend) persists a visitor UUID in `localStorage` so the same anonymous-then-logged-in person sees a consistent variant across the whole funnel.

A real testing lesson, same category as Dia 53-54's but a different root cause: the first version of the webhook-conversion test used a **hardcoded literal `subjectId` string** instead of a random UUID, and `conversion` events are deliberately not deduped (unlike `exposure`) — so running the full suite multiple times in a row (done specifically to check for the Dia 53-54 class of flakiness) accumulated multiple conversion rows against that same fixed subject across runs, breaking the test's `rows.length === 1` assertion on the 2nd+ run even though it passed the first time and in isolation. Caught the same way as Dia 53-54's flake: running the full suite repeatedly rather than once. Fixed by switching to `randomUUID()` (matching the pattern already used everywhere else in this new work) and cleaning up the polluted rows from the DB. Confirmed fixed: 4 consecutive full-suite runs landed at the same known baseline of 8 failures.

Verified for real end-to-end in-browser: forced a known visitor id through the deterministic hash to see the `discount` variant (R$47 + "Oferta de lançamento" badge) render on `/pricing`, then the `control` variant (R$57, no badge) with a different id; forced the `benefit` paywall-message variant to render on a gated audiobook's player page and clicked "Ver planos"; confirmed the real conversion event landed via `/admin/experiments`, which correctly rendered live exposure/conversion counts and rates per variant for both experiments. One honest caveat visible in that same verification: this shared dev DB has accumulated experiment events across many repeated test-suite runs today, so the `benefit` variant's displayed rate briefly showed over 100% (13 conversions counted against 5 deduped exposures) — the aggregation math itself is correct, the input data is just polluted by dev-only repeated test execution, same class of caveat already noted for Dia 53-54's user counts. Full backend suite (244 tests, up from 221) stays at the same known baseline of 8 local-only failures across repeated runs; web suite (21 tests) and lint stay green. Like the pricing/webhook work since Dia 47, none of this has been exercised against a real Stripe account yet.

**Dia 57-58 is done**: a real, minimum-viable support system, not a live-chat widget — the plan itself scopes "chat com suporte" as "mínimo: email". Migration 038 adds `support_tickets` (`user_id` nullable, so an anonymous pre-purchase question still works with just an email). `POST /api/support/tickets` uses the same `optionalAuth` middleware as Dia 49's paywall gate: an authenticated caller's own account email is used automatically (ignoring any `email` in the body), while an anonymous caller must supply a valid-shaped one. `GET /api/admin/support/tickets` / `PATCH /api/admin/support/tickets/:id` (status + an optional `adminResponse`) are admin-only, following the exact `requireAuth` + `requireAdmin` pattern already used for every other admin route. `HelpCenterPage.jsx` (`/help`, new "Ajuda" link in `Navbar`, public) combines a real FAQ (six Q&As grounded in actual product facts already documented elsewhere — Vitalício pricing, the 2 free audiobooks, the plan-aware AI rate limits, offline mode, and an honest "no subscription to cancel" answer) with the ticket form, plus a direct `mailto:` link to `suporte@techspeaking.dev` (the same address `aiFallbackService.js` has referenced since Dia 39) as the guaranteed fallback channel. `AdminSupportPage.jsx` (`/admin/support`, new "Suporte" tab) lists tickets and lets an admin write a response and mark one resolved.

Honestly scoped, not silently faked: **no outbound email is sent**. This project has never integrated an email-sending service (no SendGrid/SES/nodemailer anywhere in the codebase) — same category of gap as Anthropic/AWS/Stripe before their respective integration days. `adminResponse` is stored for reference and shown in the admin UI, but actually notifying the ticket's requester is manual today; both `API.md` and the admin page's own copy say this plainly rather than pretending a reply gets delivered. Building real email delivery wasn't in Dia 57-58's scope (the plan's own "mínimo: email" framing is about the contact channel being email-based, not about building an email-sending pipeline) and isn't a step attempted here.

Verified for real end-to-end in-browser: submitted a ticket as a freshly-registered user (also caught, while testing, that an earlier browser tab's stale/expired access token correctly produced a 400 "valid email is required" — the backend's `optionalAuth` silently treats an invalid token as anonymous, and the frontend's `isAuthenticated` check doesn't know the difference, so it omits the email field it would otherwise send; the same expired-token class of behavior already documented from Dia 48, not a new bug introduced here), then logging in fresh and resubmitting it correctly. Promoted that user to admin, confirmed `/admin/support` lists the real ticket, wrote a response, and confirmed it flips to "Resolvido" with the response text displayed. 14 new backend tests (unit: `supportTicketService`; integration: anonymous/authenticated submission, invalid email, admin list/patch, 404, non-admin rejection) all pass; full backend suite (258 tests, up from 244) stays at the same known baseline of 8 local-only failures across 3 repeated runs (checked given the Dia 53-56 flakiness lessons); web suite (21 tests) and lint stay green.

**Dia 59-60 is done, closing Etapa 4 (Semanas 9-12, Dias 41-60)**. The plan's review checklist ("Conversão aceitável? Churn baixo? CAC? LTV? LTV > 3x CAC?") can't honestly be answered yet — there's no Stripe account, no marketing spend, and no real users, so every number behind those questions is dev/test noise, not signal. `MONETIZATION_REPORT.md` (repo root, alongside `PRICING.md`) says this plainly rather than inventing plausible-sounding numbers: it walks the checklist item by item (most: "not yet answerable" with the real reason why), includes a real snapshot from `GET /api/admin/experiments/*/results` explicitly labeled as test noise (one variant showed a conversion rate over 100%, the clearest tell), and separately lists what genuinely **is** launch-ready — the checkout/webhook/experiment-attribution pipeline (Dia 47, 55-56), the analytics endpoints (Dia 53-54, 55-56), and support intake (Dia 57-58) all query real tables and need zero code changes once real traffic exists.

The actual `refactor: optimize conversion funnel` work is a real bug found by tracing the funnel end to end, not invented busywork: `LoginPage`/`RegisterPage` always hard-navigated to `/dashboard` after success, and `PricingPage`'s anonymous-visitor redirect to `/login` didn't preserve where the user came from — so anyone forced through a login detour (wanting to buy Vitalício, wanting to watch a specific audiobook) landed on the dashboard and had to re-navigate and re-click, real friction with a real drop-off risk. Fixed with the standard React Router pattern: `ProtectedRoute` and `PricingPage`'s manual redirect now pass `state: { from }`, and `LoginPage`/`RegisterPage` read `location.state?.from` instead of hardcoding `/dashboard`.

Tracing that same funnel surfaced a second, larger bug: `/audiobooks/:id/player` was wrapped in `ProtectedRoute` **unconditionally**, meaning an anonymous visitor couldn't even play "Daily Standup" or "Remote Work Communication" — the 2 audiobooks Dia 49 explicitly built free-tier backend support for (`optionalAuth`, no paywall) — without creating an account first. That directly contradicts `PRICING.md`'s own "Free — R$ 0 (trial)" pitch: a trial that requires signup before you can sample anything isn't much of a trial. Fixed by removing the route guard and making `PlayerPage` itself handle the logged-out state: the chapters fetch and the progress fetch are now independent (previously one failed `Promise.all` for both meant an anonymous 401 on `/api/user/progress` would blow up the whole page instead of just skipping progress); `saveProgress`/`handleWordClick` no-op for anonymous callers instead of firing a doomed authenticated request; `ChatWidget`/`VoiceCommandBar`/`ChapterFeedback` (all account-only) are replaced with a "Crie uma conta gratuita para salvar seu progresso e conversar com o tutor de IA" prompt instead of erroring. Word-click translation display already worked independently of the backend save (`TranscriptDisplay`'s popup is pure client-side state), so that needed no change.

Verified for real end-to-end in-browser: cleared all local auth state and loaded "Daily Standup" anonymously — audio played, transcript rendered, clicking a tagged word showed the translation popup, and the account CTA appeared instead of chat/voice/progress; loading a paywalled audiobook anonymously still correctly showed the Vitalício paywall (unaffected by removing the route guard, since that gate lives in `PlayerPage`'s own 403 handling, not the route wrapper); registered a fresh account, went anonymous again, clicked "Comprar Vitalício" on `/pricing` → redirected to `/login` → logged in → landed back on `/pricing` (not `/dashboard`) with the buy button ready, confirming the funnel fix. Added a regression test (`PlayerPage.test.jsx`) covering the anonymous-playback path specifically, since this is exactly the kind of behavior a route-guard change could silently regress later. Full web suite (22 tests, up from 21) and lint stay green; backend suite (258 tests) is unaffected (no backend changes today) and stays at the same known baseline of 8 local-only failures.

**Dia 61-62 is done, starting Etapa 5 (App Desktop Polido)**. Reviewing Dia 20's sync code for real gaps (rather than assuming it needed a rewrite) surfaced two genuine bugs and one dead feature:

1. **Conflict resolution didn't exist.** `upsertProgress` (`progressController.js`) blindly overwrote `words_learned`/`listening_count`/`completed` with whatever the client sent (`COALESCE($n, existing)` only covers *omitted* fields, not *stale* ones). A desktop client queues a progress update while offline using whatever count it last knew locally; if the same user made more progress elsewhere before the device reconnects, replaying that stale queued update would silently roll the counters backward. Fixed at the single source of truth (the backend, not duplicated per-client merge logic) with `GREATEST(submitted, existing)` for the two counters and `existing OR submitted` for `completed` — a stale push can now only leave progress unchanged, never regress it. This protects every caller (two browser tabs, web + desktop, not just the offline-sync case specifically). New `tests/integration/progress.test.js` (5 cases) proves the exact scenario: seed a higher `listening_count` server-side, then push a lower one, confirm it doesn't regress.

2. **Offline mode was half-built.** `packages/desktop/public/db.js` has cached `progress_cache`/`flashcards_cache` tables and IPC handlers (`cache:getProgress`/`cache:getFlashcards`) since Dia 20, but `desktopBridge.js` never wrapped them and no page ever called them — a network failure on desktop showed a raw error instead of falling back to the last-synced local data. Wired up end-to-end: `desktopBridge.js` gained `getCachedProgress()`/`getCachedFlashcards()`; `PlayerPage`/`FlashcardReviewPage` now fall back to the cache on a failed fetch when `isDesktop`, with `FlashcardReviewPage` showing an explicit "Offline" notice.

3. **Flashcard reviews had no offline queue at all** (only progress updates did) — found while wiring up #2, since promising "your answer will sync later" in the offline banner would have been false otherwise. Extended the exact same queue pattern Dia 20 built for progress: new `review_queue` SQLite table, `cache:queueReview` IPC handler, `sync.js`'s `pushQueuedReviews` (replays the raw quality rating through the real `/review` endpoint so the server's current SM-2 state computes the correct next interval, not a value recomputed from stale local state), and `FlashcardReviewPage` calls `queueDesktopReview` on a failed rating instead of just logging and discarding it.

Also added `SyncStatusIndicator.jsx` (Navbar, desktop-only) so the existing 60s background sync (silent since Dia 20) is finally visible — "Sincronizado (N enviados)" / "Erro de sincronização" plus a manual "Sincronizar agora" button, using `desktopBridge.js`'s new `syncNow()`/`onSyncStatusChange()` wrappers around the `sync.now`/`sync.onStatusChange` bridge methods that already existed but nothing consumed.

Verified for real: the backend conflict-resolution fix through the full integration suite against real Postgres (5 new tests, 263 total, same known baseline of 8 local-only failures across repeated runs). The desktop-side changes (`db.js`/`sync.js`/`main.js`/`preload.js`) were verified with a plain-Node functional script (same approach as Dia 20's audio-cache verification, since `packages/desktop` has no test runner and these tools can't drive a real Electron window) — registered a real user against the live local backend, seeded a higher `listening_count` server-side, queued both a stale progress update and a flashcard review locally, ran the real `syncNow()`, and confirmed: the queue drained, the stale push did not regress the counter, the queued review was genuinely applied server-side (`review_count` incremented), and `pullLatest` correctly repopulated both local cache tables. The web-side changes were verified in-browser (`isDesktop` is false there, confirming the new code paths are safe no-ops and nothing regressed for normal web usage) — full web suite (22 tests) and lint stay green. **Still unverified: an actual packaged/running Electron window** — same standing gap as Dia 20, since the available tooling only drives a Chromium tab against the Vite dev server.

**Dia 63-64 is done**: 3 of the plan's 4 notification types are real and wired end-to-end; the 4th ("Conversa IA aguardando resposta") is honestly skipped — this app's AI chat (Dia 27-28) is a synchronous request/response, there's no async "waiting for a reply" state to ever notify about, so building that notification would mean inventing a UI state that doesn't exist. `packages/desktop/public/sync.js` gained `checkForNotifications(apiUrl, token)`, called at the end of every `syncNow()` cycle: **novo audiobook disponível** (diffs the current `GET /api/audiobooks` catalog against a locally-persisted set of known ids — the first-ever sync just records the baseline instead of announcing the entire existing catalog as "new"), **flashcard para revisar** (counts `flashcards_cache` rows already due, gated by a 12h cooldown stored locally so it doesn't refire every 60s sync tick), and **streak quebrado** (compares `GET /api/user/stats`'s `streakDays` against the last-seen value; fires only on a transition from nonzero to zero). Dedup/cooldown state lives in a new generic `kv_state` table (`db.js`) — deliberately not synced to the backend, since none of it means anything outside this one local install.

`checkForNotifications` deliberately returns plain `{type, title, body}` objects instead of calling Electron's `Notification` API directly, keeping `sync.js` dependency-free of Electron (same reasoning as everything else in that file) — `main.js`'s `triggerSync` is the only place that actually shows a native OS toast, reading the list `syncNow()`'s result now carries. Every fired notification is also persisted (a new `notifications` table) — the "notification center" half of Dia 63-64's two commits: `NotificationCenter.jsx` (Navbar, desktop-only) is a bell icon with an unread-count badge and a dropdown of notification history, so a missed toast is still visible later. New IPC surface: `notifications:getAll`/`notifications:markRead`, wrapped by `desktopBridge.js`'s `getDesktopNotifications()`/`markDesktopNotificationRead()`.

Verified for real with a plain-Node functional script (same approach as Dia 20/61-62 — no Electron test runner exists) against the live local backend: registered a real user, confirmed a first-ever sync announces nothing; inserted a genuinely new audiobook directly in Postgres mid-script and confirmed the *next* sync announces exactly that one book by title, with no re-announcement on a third sync; created a real flashcard and forced its `next_review` into the past, confirmed exactly one `flashcards_due` notification fires and a second immediate sync correctly suppresses the duplicate (cooldown); forced a locally-stored `last_streak = 5` against a genuinely fresh second test user (real `streakDays` = 0) and confirmed exactly one `streak_broken` notification fires and the stored streak resets; confirmed all fired events were persisted and `markNotificationRead` correctly flips `read` in SQLite. Web-side changes (`NotificationCenter.jsx`, `desktopBridge.js` additions) verified in-browser to be safe no-ops (`isDesktop` false) with no console errors and no regression to the rest of the navbar. Full web suite (22 tests) and lint stay green; no backend changes today. **Still unverified: an actual packaged/running Electron window** (same standing gap since Dia 20/61-62) — specifically, whether `Notification.isSupported()` and the toast actually render correctly on a real OS, since these tools can't drive Electron itself.

**Dia 65 is done, closing Etapa 5's Semana 13 (Dias 61-65)**. Shortcuts are renderer-level (`useKeyboardShortcuts.js`, a `keydown` listener), not Electron's `globalShortcut` — a global OS-wide hotkey would steal these key combos from every other app on the user's machine whenever TechSpeak is merely running, which is the opposite of "professional quality" for a learning app that isn't a launcher. Gated behind `isDesktop` (same flag as Dia 61-64's other desktop-only features) so the same shared React app never hijacks a real browser tab's own Ctrl+K/L/P/R (search bar, address bar, print, reload) when running as the plain web app — this only activates inside Electron.

The plan's literal "Cmd+Space: Buscar audiobook" was adapted to **Cmd/Ctrl+K**: Cmd+Space is macOS's own reserved system-wide Spotlight shortcut, so binding it in-app would just break Spotlight instead of doing anything useful — Cmd/Ctrl+K is the standard "search" convention instead (Slack, GitHub, VS Code). The other three match the plan exactly: **Cmd/Ctrl+L** (biblioteca → `/audiobooks`), **Cmd/Ctrl+R** (flashcards → `/flashcards`), **Cmd/Ctrl+P** (play/pause — dispatches a `techspeak:toggle-playback` window event that `AudioPlayer.jsx` listens for itself, so the shortcut works without `useKeyboardShortcuts` needing to know anything about player internals, and is simply a no-op when no player is mounted). A real conflict this surfaced: Electron's *default* application menu already binds Cmd/Ctrl+R to Reload, which would fire instead of (or racing) our own flashcard-review shortcut — fixed by `main.js`'s new `setAppMenu()`: no menu bar at all in production (an end user has no reason to see one; the app is its own UI chrome), and in dev a minimal menu with Reload/DevTools moved to `Cmd/Ctrl+Shift+R`/`Cmd/Ctrl+Shift+I` so they don't collide with the app's own shortcuts.

`AudiobookListPage.jsx` gained a real client-side title search box (25 audiobooks is enough that this genuinely helps, not just a showcase for Cmd+K) — Cmd+K navigates there and focuses it via the same `location.state` pattern Dia 59-60's login-redirect fix already established. `KeyboardShortcutsHelp.jsx` (Navbar, desktop-only) is the second commit's "shortcuts help menu": a small modal listing all 5 bindings (the 4 above plus itself, opened either by its own button or **Cmd/Ctrl+/**, a common "show shortcuts" convention) — a shortcut nobody can discover isn't very useful.

Verified: `useKeyboardShortcuts.test.js` (6 new tests, toggling `window.techspeak` + `vi.resetModules()` per case since `isDesktop` is a module-level constant) confirms the hook is inert outside the desktop app and each binding does the right thing inside it; full web suite (28 tests, up from 22) and lint stay green. Verified in-browser (`isDesktop` false there) that the search box, filtering, and the rest of the app behave exactly as before with no console errors — the shortcuts/help button are correctly absent. `main.js`'s `Menu`/`Notification`-dependent code (like all Electron-only code in this project) can't be verified by these tools, which only drive a Chromium tab against the Vite dev server, not `electron .` — same standing gap as Dia 20/61-64. No backend changes today.

**Dia 66-67 is done, starting Etapa 5's Semana 14**. `ThemeContext.jsx` (mirroring `AuthContext.jsx`'s existing pattern) is a three-state theme (`light`/`dark`/`system`) persisted to `localStorage` (`techspeak_theme`), computing `isDark` from either the explicit choice or `window.matchMedia('(prefers-color-scheme: dark)')` (with a live `change` listener for "system" mode), and toggling a `dark` class on `document.documentElement`. Tailwind v4's `dark:` variant defaults to `prefers-color-scheme` only with no manual override — `@custom-variant dark (&:where(.dark, .dark *));` in `styles/index.css` is what makes it also respond to that class, so `ThemeToggle.jsx` (Navbar, cycles light→dark→system) can force either mode regardless of OS preference everywhere `dark:` is already used.

Color choices were asked, not assumed, per [[project_theme_color_preference]]: the user picked **blue** as the primary/brand color (already-used `blue-600`/`blue-500`) and **warm/soft gray** (Tailwind's `stone` palette, not the cooler default `slate`/`zinc`) for dark-mode surfaces — rolled out via `body`'s base `dark:bg-stone-900 dark:text-stone-100` plus `dark:` variants on every explicit background/border/badge color across ~30 files (verified via `Grep` before each bulk `sed` replacement that every match was a legitimate same-treatment target). Mid-rollout the user sent a reference image (a neon dark-UI mockup) and asked for buttons to have a "light neon, tech" feel — added `.neon-glow`/`.neon-text` CSS classes (`box-shadow`/`text-shadow` glow, dark-mode-only via `.dark .neon-glow` since a glow doesn't read as neon against a white light-mode background) applied to every primary blue button/active-state/the user's own chat bubble, and to the "TechSpeak" wordmark.

A real accessibility regression was caught the same way this project has caught real bugs before: not by the automated suite (Vitest doesn't assert on visual contrast) but by taking a live screenshot of `PlayerPage` in dark mode and seeing the "Anterior"/"Próximo" buttons render as a near-invisible light-gray box. Root cause: pre-existing light-background utility classes (`bg-slate-100`, `bg-amber-50`, `bg-slate-50`, `bg-green-50`) had no `dark:` variant, so they kept their light-mode fill (and often light-mode-appropriate text color) against the new dark canvas. Fixed systematically — `Grep`-swept the whole `src` tree for each pattern, not just the one page the screenshot happened to catch — across `PlayerPage`, `AudioPlayer`, `ChatWidget` (message bubbles, fallback FAQ box, feedback buttons, typing indicator), `AdminLayout`, `AdminContentPage`, `KeyboardShortcutsHelp`, `ChapterFeedback`, `AdminRevenuePage`, the four admin table-header pages (`AdminAnalyticsPage`/`AdminMetricsPage`/`AdminExperimentsPage`/`AdminUsersPage`), `NotificationCenter`, `AudiobookProgressList`, `RecommendationsFeed`, `HelpCenterPage`, and `PricingPage`.

Also added a small `RobotLogo.jsx` mascot icon (red robot head, round ears on the sides, blue eyes, transparent background so it always blends with the page instead of carrying its own dark backdrop) placed before the "TechSpeak" wordmark in `Navbar` — the user's own follow-up request for a red robot (contrasting the already-blue brand text, Americana-flag-style) with blue eyes.

Verified for real in-browser in both themes: the toggle cycles light→dark→system→light correctly, the choice survives a reload (`localStorage`/`document.documentElement.classList` checked directly), every `dark:`-fixed component renders with correct contrast, the neon glow appears only in dark mode, and the robot logo's red/blue/transparent-background treatment holds in both light and dark. `npx prettier --write`, `npm run lint` (0 errors, same 2 pre-existing warnings), and `npm test --workspace=packages/web` (28/28) all pass; full backend suite (255 pass / 8 fail, same known local-only baseline) is unaffected — no backend files touched, this is pure shared-React-app CSS/Context work that also applies inside Electron's Chromium-based renderer with no separate desktop code needed.

Not done: no live verification inside an actual packaged/running Electron window (same standing gap since Dia 20) — specifically whether a v8/system dark-mode preference syncs correctly with Electron's `nativeTheme`, since `ThemeContext`'s "system" mode currently only reads the renderer's own `matchMedia`, which should already be correct behavior for Electron's Chromium renderer but hasn't been directly observed running as `electron .`.

**Dia 68-69 is done, closing Etapa 5's Semana 14 dark-mode/theming arc (Dias 66-69)**. The plan's three "Temas Customizáveis" options were: light/dark (already Dia 66-67), primary color, and font size — this day adds the latter two, plus the plan's explicit `backend: save user theme preferences` commit, which Dia 66-67 didn't need (light/dark/system is a pure display preference with no cross-device meaning; color/font-size is a deliberate choice a user reasonably expects to follow them to another device).

Migration 039 adds `users.theme_primary_color` (default `'blue'`) and `users.theme_font_size` (default `'medium'`). `PATCH /api/user/theme` (auth required, `services/themeService.js`) validates against a fixed set (`blue`/`purple`/`green`/`red`; `small`/`medium`/`large`) and persists both; `toPublicUser` (auth controller) and `GET /api/auth/me` now include `themePrimaryColor`/`themeFontSize` on every user object, the same pattern `plan`/`isAdmin` already follow.

Making "primary color" actually repaint the app (not just a stored preference nobody reads) required centralizing what Dia 66-67 had left as ~16 files' worth of literal `bg-blue-600 dark:bg-blue-500 neon-glow` / `text-blue-600 dark:text-blue-400`: `styles/index.css` now defines `--color-primary`/`--color-primary-dark`/`--color-primary-glow` CSS variables (default blue, overridden by `:root[data-primary='purple'|'green'|'red']`) plus `.text-primary`/`.bg-primary`/`.border-primary` utility classes and updated `.neon-glow`/`.neon-text` to read the same `--color-primary-glow` variable instead of a hardcoded blue rgba — a `Grep`-verified `sed` sweep replaced every matching button/link/wordmark class with the new `-primary` classes, so one attribute on `<html>` now repaints every primary-colored element app-wide. `RobotLogo`'s blue eyes were deliberately left hardcoded — the mascot's own red/blue color scheme is fixed brand identity, not meant to shift with the user's chosen accent color. Font size uses an even smaller mechanism: since Tailwind's `text-*` utilities are rem-based and this project has no `tailwind.config.js` overriding that, setting `document.documentElement.style.fontSize` directly (14px/16px/18px) scales every rem-sized element app-wide with zero component changes needed.

`ThemeContext.jsx` gained `primaryColor`/`fontSize` state (persisted to `localStorage` immediately, like `theme` already was) plus `setPrimaryColor`/`setFontSize`, which additionally `PATCH` to the backend when authenticated. A logged-in user's stored preference is adopted once per login (tracked by user id, not on every `user` object update — `refreshUser()` after a payment, for instance, shouldn't clobber a preference change made earlier in the same session) via React's "adjust state during render" pattern rather than a `useEffect`, since the latter tripped the project's `react-hooks/set-state-in-effect` lint rule (same class of cascading-render issue this project has avoided by design since Dia 66-67). This required reordering `App.jsx`'s providers — `AuthProvider` now wraps `ThemeProvider` (previously the reverse) — since `ThemeContext` needs `useAuth()` to know the caller's id/token. `ThemeSettings.jsx` (Navbar, next to the existing light/dark toggle) is a small popover with 4 color swatches and 3 font-size buttons.

Verified for real end-to-end: `theme.test.js` (5 new integration tests: auth-required, defaults, both validation-rejection cases, persistence) all pass against real Postgres, plus the full `auth.test.js` suite re-run alongside them to confirm the `toPublicUser` change didn't regress login/register/me (260 pass / 8 fail total, the exact same known local-only baseline, up from 255). In-browser: registered a fresh test user, confirmed the wordmark/buttons repaint live when switching colors and font sizes (screenshotted at each step), confirmed both persist across a real reload, then specifically tested the cross-device-sync path — cleared all local storage, logged back in fresh, and confirmed the account's saved red/medium preference was adopted immediately from the server response, not a stale local default. Caught and fixed a real lint error before any of this (`react-hooks/set-state-in-effect` on the login-hydration logic) by switching from a `useRef`+`useEffect` pattern to the render-phase state-adjustment pattern. `npm run lint` (0 errors, 4 known-class warnings), `npm test --workspace=packages/web` (28/28) both green; test user cleaned up from the dev DB afterward.

Not done: no UI affordance to reset to defaults (a user can always click "Azul"/"Média" manually, so not a real gap); the 4-color/3-size option set is fixed, not a free color picker — matches the plan's own "cores primárias" framing (a curated set) rather than an arbitrary-hex picker, which would also need real contrast validation against both light and dark canvases to avoid shipping an unreadable combination.

**Dia 70 is done, closing Etapa 5 (Semanas 13-14, Dias 61-70)**. The plan's checklist (ARIA labels, color contrast/WCAG AA, keyboard navigation, screen reader compatibility) was tackled with two different tools for two different classes of bug: `jest-axe` (new dev dependency, wraps axe-core for Vitest) for anything jsdom can check statically — labels, roles, landmarks — and, since jsdom doesn't do real layout/paint so axe's `color-contrast` rule can't run there, a live `axe-core` audit injected directly into the running Vite dev server via the Browser pane (axe-core copied from `node_modules` into `public/` temporarily, loaded with a `<script>` tag, removed again before committing) to catch real rendered contrast failures across every theme combination.

**ARIA labels**: 17 form inputs across 7 files (`LoginPage`, `RegisterPage`, `HelpCenterPage`, `ChatWidget`, `AdminUploadPage`, `AdminSupportPage`, `AdminContentPage`, `AudiobookListPage`) relied on `placeholder` alone with no `<label>` or `aria-label` — a real failure of WCAG 1.3.1/3.3.2/4.1.2, since placeholder text disappears on input and isn't reliably announced by screen readers. Every one got an explicit `aria-label` matching its placeholder.

**Color contrast (WCAG AA)** surfaced the day's most substantial, genuinely surprising finding: Dia 68-69's `--color-primary-dark` variable was reused for *both* dark-mode text (wordmark, links, active nav) and dark-mode button backgrounds (`bg-primary`, white text on top) — and no single Tailwind shade satisfies 4.5:1 for both at once. Verified with the actual WCAG relative-luminance formula (run in-browser, not eyeballed): e.g. red-500 is 4.65:1 as text-on-stone-900 but only 3.76:1 as a white-text button background; green-600 fails white-text contrast in *either* theme at only 3.30:1. Fixed by splitting the variable's job: `--color-primary` (a *-600/*-700 shade, green needed 700 not 600) now serves buttons/borders in **both** themes since those shades already clear 4.5:1 for white text universally; a new, narrower `--color-primary-dark` (*-400 shades) serves dark-mode **text only**. Also fixed along the way: the wordmark's white "ak" letters used a permanent dark `-webkit-text-stroke` (added Dia 66-67 so they'd read on any background) — axe correctly flagged this as low-contrast in dark mode, because the stroke blends with the fill in contrast calculations even though it's visually fine; the stroke is now light-mode-only (`dark:[-webkit-text-stroke:0px]`), since dark mode's plain white already has ~15:1 contrast against stone-900 without it. A live 4-color × 2-theme matrix (16 combinations) confirmed clean on Login, Register, Home, Help, AudiobookList, Pricing, Dashboard, and the Player.

A second, independent systemic sweep (unrelated to the primary-color work) fixed every bare/unpaired muted-text utility across the whole `src` tree that Dia 66-67's original dark-mode pass had missed: `text-slate-500`/`600`/`700`/`900` and `text-red-500` appeared in ~50+ places with no `dark:` variant at all, some literally unreadable (a `text-slate-900` link sitting directly on the page body hit near-zero contrast once the body went dark; `text-slate-700` on `PricingPage`'s feature list measured 1.68:1 in dark mode - close to invisible). `text-slate-400`, which turned out to be too light even in **light** mode (2.63:1 against white), got promoted to the same `text-slate-500 dark:text-stone-400` pairing used everywhere else, except in `TranslationPopup.jsx` (deliberately excluded — its background is a fixed `bg-slate-900` regardless of app theme, a different, already-correct context). Two more real bugs, not contrast-related but only found *because* of this pass: `AudioPlayer`'s inactive speed buttons (0.7x/0.8x/etc) silently inherited the muted `text-slate-500` from their ambient label-row wrapper instead of getting their own readable color (4.35:1, failing); and — the one bug this day's own contrast fix introduced — `TranscriptDisplay`'s "currently playing" word highlight (`bg-yellow-200`) and hover state (`hover:bg-slate-200`) both inherited the *newly-lightened* `dark:text-stone-200` transcript text color, making highlighted/hovered words nearly invisible against their own bright-yellow/light-slate backgrounds (1.07:1) — fixed by forcing `text-slate-900` specifically on those two light-background states, since they're always light regardless of app theme.

**Keyboard navigation**: `KeyboardShortcutsHelp`'s modal gained `role="dialog"`/`aria-modal`/`aria-labelledby`, an Escape-to-close handler, and focus management (moves to the close button on open, returns to the trigger on close) — previously mouse-only, with no way for a keyboard user to leave it except tabbing blindly past the backdrop. `NotificationCenter` and `ThemeSettings` (both simple popovers, not modals) gained `aria-haspopup`/`aria-expanded`, Escape-to-close-and-refocus-trigger, and click-outside-to-close, none of which existed before — previously the only way to close either was clicking its own trigger button again. `Layout.jsx` gained a skip-to-content link (`sr-only` until focused, jumps to a new `id="main-content"` `tabIndex={-1}` on `<main>`), letting a keyboard user bypass the repeated Navbar/Sidebar links instead of tabbing through them on every single page.

Verified for real, not just written: `npx axe-core` matrix confirmed 0 violations across all 4 primary colors × light/dark on 8 pages; the skip link was tested by pressing Tab from a blank page load and confirming it becomes visible and focusable, then confirmed via a real click (not a simulated Enter keypress, which the browser-automation tool doesn't dispatch as a genuine link activation) that focus and the URL hash both land on `#main-content`; `ThemeSettings`'s Escape/click-outside/focus-return was exercised live in-browser via `aria-expanded` and `document.activeElement` checks, not just assumed from the code. 9 new `jest-axe` tests (`tests/a11y.test.jsx`: HomePage, LoginPage, RegisterPage, HelpCenterPage, AudiobookListPage, Navbar logged-in/out) and 3 new keyboard-behavior tests (`tests/keyboardNav.test.jsx`) all pass; full web suite (38 tests, up from 28) and lint stay green. No backend changes today — this was entirely a frontend/CSS/markup pass.

Not done, honestly: full manual screen-reader testing (NVDA/VoiceOver) wasn't performed — this project's environment has no way to drive a real screen reader, so "screen reader compatibility" here means correct semantic HTML/ARIA (verified by axe's ruleset, which specifically encodes screen-reader-relevant rules) rather than an actual listen-through; `NotificationCenter`/`KeyboardShortcutsHelp`'s new keyboard behavior couldn't be verified live in-browser the same way `ThemeSettings` was, since both are gated behind `isDesktop` (`window.techspeak`) and these tools only drive a regular browser tab — their correctness rests on the automated test + the fact that they share the exact same Escape/focus-return code pattern already confirmed working live in `ThemeSettings`. Admin pages (behind login + a 15-minute JWT that expired mid-session) weren't re-verified live in-browser after the fixes, though every admin file's bare-color-class instances were caught and fixed by the same grep-verified sweeps applied everywhere else.

**Dia 71-72 is done, starting Semana 15**. The plan's tool choice (K6/JMeter) was swapped for `autocannon` — a pure-Node HTTP load generator, installable with a plain `npm install` like every other dependency in this project, instead of a separate Go/Java binary. `packages/backend/loadtest/run.js` (plus `helpers.js` for setup) targets this machine's real running dev backend, not an ephemeral test server, since the point is measuring actual server behavior under concurrency — the same question K6/JMeter would answer against a real deployment. New `npm run test:load --workspace=packages/backend` script; deliberately excluded from the regular `npm test` discovery (lives in `loadtest/`, not `tests/`, same pattern as Dia 35's `test:ai-quality`).

"1000 usuários simultâneos" was interpreted honestly, not literally applied to every endpoint: simulated as concurrent **read** traffic (catalog browsing, authenticated dashboard reads) — what 1000 simultaneous users of a content app mostly means — rather than 1000 concurrent `/api/ai/explain` calls. `AI_DAILY_RATE_LIMIT` (Dia 37) exists specifically to cap concurrent AI cost, and with no local Redis (same standing gap since Dia 30) that limiter fails open on this machine, so blasting the AI endpoint at real concurrency would mean 1000 real, real-money Anthropic calls with no informative signal beyond "yes, an unthrottled loop spends money fast." Instead, 3 real AI calls run *concurrently with* the heavy read-traffic scenario, honestly answering "does AI stay responsive under load" without reckless spend.

Real results (`packages/backend/PERFORMANCE.md`, the `docs: add performance benchmarks` deliverable): public catalog/chapters endpoints handled 100 concurrent connections at 3,200-4,100 req/s with p50 24-29ms and zero errors; `GET /api/user/stats` at **1000** concurrent connections handled 1,636 req/s with zero errors/timeouts but p50 latency of 568ms — ~20x worse than the simpler endpoints at 1/10th the concurrency. Root-caused, not guessed: `statsService.getUserStats` runs 5 separate queries per request, and `config/database.js`'s `pg.Pool` has no explicit `max` set (defaults to 10) — at 1000 concurrent × 5 queries, requests almost certainly queue for one of only 10 DB connections rather than failing, which is a connection pool doing exactly what it's supposed to under overload (degrade, don't error) but is the real, measured explanation for the latency cliff. The 3 concurrent AI calls all succeeded (200 OK, 3.4-4.4s — slower than Dia 35/40's idle baseline of ~1.2-2.5s, consistent with sharing the same Node process's event loop/DB pool with the 1000-connection burst, not an AI-path failure). Documented, not fixed — the pool-size/query-count finding is explicitly flagged for Dia 73-74 ("Otimização de Performance"), which owns `perf: improve database queries`.

Honest caveats spelled out in `PERFORMANCE.md` itself: single dev machine, not a production topology (no load balancer, read replicas, or CDN); no local Redis means the AI cache/rate-limit paths weren't really exercised; only read traffic was load-tested this round, not writes (progress upserts, flashcard reviews), which have different bottlenecks (lock contention, not just pool queueing) worth a future pass. Full backend suite (268 tests, same 260 pass / 8 known-local-failure baseline) confirmed the new `loadtest/` files aren't accidentally picked up by `npm test`'s auto-discovery; lint stays green. No web-side changes today.

**Dia 73-74 is done**. Four planned areas (image optimization, minification, service worker, database indexing) tackled honestly against what's actually true in this app today.

**Database** (`perf: improve database queries`, committed first): closed the loop on Dia 71-72's own finding rather than guessing at a fix. `EXPLAIN ANALYZE` against the real DB showed every one of `getUserStats`'s 5 queries executing in under 1ms even under the exact load that showed degraded latency, and the query planner correctly choosing sequential scans over the existing indexes given this dataset's small table sizes — so adding more indexes would not have helped, a real negative result worth having instead of assuming. The actual cause: `pg.Pool` had no explicit `max` (node-postgres defaults to 10) against 1000 concurrent requests each needing up to 5 simultaneous connections. Fixed with `max: 20` (`DB_POOL_MAX`-overridable) and merging two `statsService` queries that scanned the same `word_clicks` rows with the same `user_id` filter, cutting the endpoint to 4 queries. Re-measured with the identical Dia 71-72 load test: +14% req/s, -8% p50 latency — real, but honestly modest, since 1000 concurrent requests against one bounded pool will always queue somewhere.

**Bundle size** (`perf: optimize bundle size`): `PlayerPage` was already the largest lazy-loaded route chunk even after Dia 23's code-splitting (21.83kB / 6.61kB gzip). `PronunciationRecorder`, `ChatWidget`, `ChapterFeedback`, and `VoiceCommandBar` — all secondary to the core playback/transcript experience, and three of the four only relevant to authenticated users — are now `React.lazy()`-loaded within `PlayerPage` itself instead of bundled eagerly into its chunk. Measured, not assumed: PlayerPage's own chunk dropped to 10.91kB / 3.96kB gzip (**-40% gzip**), with the four extracted pieces now their own small, independently-cacheable chunks. Verified live in-browser (screenshot + real network trace) that nothing regressed — pronunciation recorder, voice command bar, and chat widget all still render and function correctly. Minification was verified already active (Vite's esbuild-based production build, confirmed by inspecting the actual minified output), not something that needed adding.

**Service Worker / PWA** (`perf: add service worker`) has a real story worth documenting honestly: `vite-plugin-pwa` (the standard choice) was tried first, but both of its strategies (`generateSW` and `injectManifest`) internally spin up a classic-Rollup build step to bundle the service worker, and this dev machine's Windows **Application Control policy blocks the native `@rollup/rollup-win32-x64-msvc` binary** that step needs — confirmed by trying both strategies and hitting the identical blocked-DLL error both times, not a code bug on either side. Rather than trying to work around an OS security policy (not something to bypass), pivoted to a fully **hand-written, zero-build-step service worker** in `packages/web/public/sw.js` — Vite copies `public/` verbatim with no bundling, sidestepping the blocked binary entirely. Trade-off stated plainly in the file's own comment: no exact precache manifest of this build's hashed filenames (that needs the build-time injection this environment can't produce); instead a runtime "cache what's actually been visited" strategy — network-first for navigations (falls back to the last cached shell if offline), cache-first for hashed static assets (safe since content-hashed filenames are immutable). Deliberately does not touch `/api/*` (must always be fresh) or audio files (external storage, `packages/desktop` already owns real offline audio caching - Dia 20). `packages/web/public/manifest.webmanifest` plus four real PNG icons (`pwa-192.png`, `pwa-512.png`, `apple-touch-icon.png`, `favicon.png`) rendered from the actual `RobotLogo` shape via a one-off `sharp` script (installed, used, then uninstalled — not a permanent dependency for a one-time asset-generation task), not a placeholder image. `registerServiceWorker.js` only registers in production builds (`import.meta.env.PROD`), since a service worker intercepting fetches in dev would fight Vite's own HMR. Verified for real: built the production bundle, served it with `vite preview`, confirmed in-browser via `navigator.serviceWorker.getRegistrations()` that the worker registers and activates, confirmed the manifest parses correctly via a real `fetch`, and confirmed the cache actually populates with the real app shell (root document, vendor/index JS, CSS) after a genuine navigation.

**Image optimization**: same honest conclusion as Dia 23 — this app has no cover-art/content images yet (no upload/display feature for them exists), so there was nothing to optimize there. The only new images this day are the four small PWA icons themselves, already minimal flat-color PNGs with nothing further to compress.

Full web suite (38 tests) and lint stay green throughout; `npm run build` succeeds cleanly. No backend test regressions (260 pass / 8 known-baseline fail, unchanged). `npm audit` is back to just the one already-tracked react-router advisory (flagged for separate review at Dia 68-69) after the temporary `sharp`/`vite-plugin-pwa` installs were removed again.

**Dia 75 is done, closing Semana 15 (Dias 71-75)**. Started with a background-agent audit of what was already true rather than assuming gaps: SQL injection risk is effectively zero (every one of the ~24 files calling `pool.query` across the whole backend uses `$1`/`$2` placeholders; the one template-literal exception in `audiobookController.js` interpolates a hardcoded module constant, never request input) and XSS risk in the frontend is zero (no `dangerouslySetInnerHTML`/`innerHTML`/`document.write` anywhere in `packages/web/src` — React's default JSX escaping is the only rendering path). Both items on the plan's checklist needed verifying, not fixing.

The real gaps were the ones `CLAUDE.md` had already been honestly flagging since Dia 25/49: no CORS origin restriction, and no general (non-AI) rate limiting. `helmet()` now sets standard security headers on every response; `cors()` is restricted to `FRONTEND_URL` (already used for Stripe redirects) instead of the previous wide-open default, with requests carrying no `Origin` header (curl, the desktop app's main process) still let through, since CORS is a browser-enforced mechanism that can't meaningfully constrain non-browser clients either way. `middleware/security.js`'s `enforceHttps` redirects plain HTTP to HTTPS and sets `Strict-Transport-Security`, gated on `NODE_ENV=production` so local dev and the test suite (plain `http://localhost`) are unaffected — checks both `req.secure` and `X-Forwarded-Proto`, since a real deployment almost certainly sits behind a TLS-terminating reverse proxy. `middleware/generalRateLimit.js` adds two new `express-rate-limit` limiters (in-memory, per-IP — a real scaling gap for a future multi-instance deployment, same class already accepted for Dia 37's Redis-backed AI limiter): `GENERAL_RATE_LIMIT` (default 300/15min) across all of `/api/*`, and a tighter `AUTH_RATE_LIMIT` (default 20/15min) specifically on `/api/auth/*` against login/register brute-forcing — both read fresh per-request via a function, not baked in at module load, so tests can override them to a tiny number instead of making hundreds of real requests.

Input validation: `utils/validation.js` centralizes `isValidEmail`/`isValidPassword` (`supportController.js` had its own private copy of the same email regex since Dia 57-58 — now shares one). `POST /api/auth/register` previously accepted any truthy string as an email or password (a single-character password was valid) — now rejects a malformed email or a password under 8 characters with a 400, while `login` still only checks presence (a login shouldn't retroactively enforce today's policy against an account created under an older one).

Verified for real: a dedicated `tests/integration/security.test.js` (8 cases) hits a real running server for every new behavior — confirms `helmet`'s headers are actually present, that the configured origin is allowed and an attacker origin isn't, that no-Origin requests still work, that both new rate limiters actually 429 once their (tiny, test-overridden) limit is hit, and that `NODE_ENV=production` actually triggers the 308 HTTPS redirect while dev/test doesn't. A real ordering bug was caught by running the full file, not each test in isolation: `express-rate-limit`'s in-memory store is a single module-level singleton shared across every `createApp()` call within one test *process* (Node's test runner isolates by file, not by test) — an earlier describe block's incidental `/api/health` calls had already partially consumed a later block's deliberately tiny quota, causing an immediate 429 instead of the expected 200s; fixed by ordering the tiny-limit-override tests first in the file, before anything else touches the same rate-limited route. `tests/unit/validation.test.js` (7 cases) covers the pure validation functions directly; 2 new `auth.test.js` cases confirm the register-endpoint validation end-to-end. Full backend suite (285 tests, up from 268) stays at the same known baseline of 8 local-only failures (`ANTHROPIC_API_KEY` configured + no local Redis, unchanged since Dia 30/37); web suite and lint untouched today (backend-only day).

Not done, honestly: the two new rate limiters are in-memory (per-process), not Redis-backed like Dia 37's AI limiter — correct for this single-instance deployment, but would under-count across a future multi-instance/load-balanced deployment (documented as a known limitation, matching the AI limiter's own accepted gap). CORS's allow-list is a single origin (`FRONTEND_URL`), not a list — fine for one web frontend, would need extending if a second legitimate origin (e.g. a staging domain) is added later. HTTPS enforcement is written and tested but never exercised against a real TLS-terminating proxy, since no hosting provider is chosen yet (same standing gap as `deploy-staging.yml`'s own TODO).

**Dia 76-77 is done, starting Semana 16**. "Recrutar 50-100 beta testers" is a real-world marketing/outreach action outside what code can do (same class of thing as "responder perguntas de usuários reais" throughout this project - there's no real user base yet) - what's buildable, and built, is the actual infrastructure a beta program needs: a way to grant free Pro access, and a way to collect feedback.

Migration 040 adds `users.is_beta_tester` (boolean, default `false`) and `beta_feedback` (category, optional 1-5 rating, message, and `was_beta_tester` — a **snapshot** of the submitter's beta status *at submission time*, not a live join, so a later promotion/revocation doesn't retroactively relabel old feedback). `services/betaProgramService.js`'s `setBetaTesterStatus` deliberately reuses the existing `plan = 'pro'` value instead of introducing a separate `'beta'` plan — granting beta status sets `plan = 'pro'` directly, so every existing `plan === 'pro'` check across the app (Dia 49's paywall, Dia 37/49's AI rate limits, the Navbar/dashboard badges) already works correctly with zero changes elsewhere. Revoking beta status deliberately does **not** revert `plan` back to `'free'`: this app has no purchase-history table (Dia 47's Stripe webhook sets `plan='pro'` directly, nothing records *how* someone got there), so there's no reliable way after the fact to tell a beta grant apart from a real purchase — auto-downgrading on revoke would risk yanking access from someone who has since paid for real.

`PATCH /api/admin/users/:id/beta-tester` (admin-only, mirrors the existing `is_admin` toggle pattern exactly) grants/revokes; `AdminUsersPage.jsx` gained a "Beta" column and toggle button next to the existing admin one. `POST /api/beta/feedback` (auth required, any authenticated user — not gated to current beta testers, since general product feedback is valuable from anyone and the snapshot field keeps beta feedback distinguishable) accepts a category (`bug`/`feature_request`/`general`), an optional 1-5 rating, and a message; `FeedbackPage.jsx` (`/feedback`, new Navbar link for authenticated users) is a small dedicated form — deliberately separate from Dia 57-58's `HelpCenterPage` support-ticket flow, since this is open-ended product feedback, not a specific help request expecting a reply. `GET /api/admin/beta-feedback` + `AdminBetaFeedbackPage.jsx` (`/admin/beta-feedback`, new "Feedback Beta" admin tab) lists every submission with the submitter, star rating, and category badge. A small "Beta Tester" badge was also added next to the existing "Vitalício" badge in the Navbar for recognition.

Verified for real, end-to-end in-browser: registered a fresh test user, submitted real feedback (bug report, 3-star rating) *before* being granted beta status, promoted a separate admin account, used the new Beta toggle to grant the test user beta status and confirmed their `plan` flipped to `pro` live in the Usuários table, then opened the new Feedback Beta tab and confirmed the earlier submission displays correctly with **no** "beta tester" label — the exact proof that the snapshot is taken at submission time, not computed live from current status. 13 new integration tests (`tests/integration/beta.test.js`) cover the full matrix (auth/admin gating, validation, the grant-also-sets-plan behavior, and the revoke-does-not-downgrade behavior) against real Postgres; a real bug was caught immediately by running the full suite rather than just the new file — `tests/unit/adminUserService.test.js`'s `listUsers` mock didn't include the new `is_beta_tester` column, breaking a previously-passing test the moment the SQL/mapping changed, fixed by updating the mock row and expected output. Full backend suite (298 tests, up from 285) stays at the same known baseline of 8 local-only failures; web suite (38 tests) and lint stay green; test user cleaned up from the dev DB afterward.

Not done, honestly: no actual outreach/recruitment happened (that's a real-world marketing action, not a code task); no invite-code/pre-registration flow exists — an admin grants beta status to an *already-registered* account, mirroring the existing `is_admin` promotion pattern rather than building a separate invite mechanism nothing else in this app has; no automated notification tells a user they've been granted beta access (same class of gap as Dia 57-58's support tickets — no email-sending service integrated yet).

**Dia 78-79 is done, closing Semana 16**. The plan frames this as "Bug Fixing Sprint" against real beta-user reports — there is no real beta user base yet (same honest caveat as every prior "real users" gap in this project), so a background-agent audit re-read the accumulated codebase for genuine bugs instead of triaging tickets that don't exist. 4 real, concrete bugs were found and fixed, all pre-existing (none introduced this session):

1. **`progressController.js`'s `saveWordClick`** (Dia 61-62) ran 3 sequential writes — `word_clicks` insert, `flashcards` insert, `user_progress` upsert — with no transaction wrapping the same class of bug `adminAudiobookController.js` (Dia 43) already guards against for its own multi-insert. A failure on the 2nd or 3rd write left the 1st already committed, an inconsistent partial state. Fixed with the exact same `pool.connect()`/`BEGIN`/`COMMIT`/`ROLLBACK`/`finally client.release()` pattern already established there. New integration test (`player.test.js`) forces a real FK violation (a non-existent `wordId`) and confirms the request 500s **and** leaves zero rows in `word_clicks` — proving the rollback actually undoes the first insert, not just that an error is returned.

2. **Desktop `sync.js`'s `pushQueuedUpdates`/`pushQueuedReviews`** (Dia 20/61-62) had no per-item error handling in their queue-drain loops — one permanently-failing entry (a stale chapter/flashcard id from a since-deleted server row, for instance) threw out of the loop, which skipped every other queued item **and**, since `syncNow`'s outer `try/catch` wraps the whole sequence, skipped `pullLatest()`/`checkForNotifications()` too — every future sync would hit the same poison-pill entry first and never make progress again. Fixed by wrapping each loop iteration in its own `try/catch`: a failing entry stays queued for a later retry instead of being lost, but no longer blocks its siblings or the rest of the sync cycle.

3. **Desktop `db.js`'s `replaceCachedProgress`/`replaceCachedFlashcards`** (Dia 20) did `DELETE` then a reinsert loop with no transaction — a failure partway through the loop (or the process dying mid-loop) left the local offline cache empty instead of either the old or new data, exactly the scenario this cache exists to protect against (an offline user reopening the app). Fixed by wrapping both in `db.exec('BEGIN'/'COMMIT'/'ROLLBACK')` (`node:sqlite`'s `DatabaseSync` supports the same statements as any SQLite connection).

4. **Electron `main.js`'s `mainWindow`** (Dia 20) was never nulled when the window closed — on macOS the app process stays alive after the last window closes (`window-all-closed` only quits on other platforms), so the 60s auto-sync's `triggerSync()` would call `.webContents.send()` on a destroyed-but-non-null `BrowserWindow` reference and throw. Fixed with a `'closed'` listener that resets `mainWindow = null`, plus an explicit `!mainWindow.isDestroyed()` guard in `triggerSync` before sending.

Verified for real: fix #1 via the new integration test against real Postgres (full backend suite: 299 tests, up from 298, 291 pass / 8 fail — same known local-only baseline, no regressions). Fixes #2/#3 via a plain-Node functional script (same established pattern as Dia 20/61-64, since there's no Electron test runner) run twice against the real local backend and a temp SQLite file: forced a duplicate-primary-key insert mid-reinsert-loop and confirmed the original cached rows survive untouched (rollback, not data loss); queued a progress update against a non-existent chapter and a review against a non-existent flashcard, ran a real `syncNow()`, and confirmed it returned `ok: true` with both poison-pill entries still queued for retry — and that `pullLatest`/`checkForNotifications` still ran (a real `notifications` array came back) despite the queue failures; ran a second `syncNow()` cycle and confirmed it completes normally instead of wedging. Fix #4 (Electron main-process code) could only be reviewed, not exercised — same standing gap as every Electron-only change since Dia 20, since available tooling drives a Chromium tab against the Vite dev server, not `electron .`. Two lint errors introduced by the `sync.js` fix (unused `err` bindings in empty catch blocks) were caught by the full `npm run lint` run and fixed (bare `catch {}`) before committing — full lint (0 errors, same 4 known warnings) and Prettier stay green.

Not done, honestly: no new bugs were found in `packages/web`'s React code this pass (the audit's findings clustered in backend transaction-safety and Electron/desktop sync, not the frontend); this is a one-time re-read rather than a recurring practice, so future days could still surface more of the same class of issue.

**Dia 80 is done, closing Etapa 5 (Semanas 13-16, Dias 61-80)**. The plan's 11-item pre-launch checklist is answered honestly in the new [LAUNCH_CHECKLIST.md](LAUNCH_CHECKLIST.md), item by item, with today's real verification runs rather than a recap of old day entries.

Two items got genuinely new, buildable work rather than just a status check: **backup system** and **disaster recovery** had zero infrastructure before today (`grep`-confirmed: no `pg_dump`/backup reference existed anywhere in the repo). `packages/backend/scripts/backup.js` (`npm run backup`) runs `pg_dump -Fc` against `DATABASE_URL` into a gitignored `backups/` directory; `scripts/restoreTest.js` (`npm run backup:verify`) is the part that actually matters — an untested backup isn't a real backup — it restores the latest dump into a throwaway database on the same Postgres instance (never the real dev DB), compares row counts against the source for `users`/`audiobooks`/`chapters`/`words`/`flashcards`, and drops the throwaway DB in a `finally` block regardless of outcome. Run for real against this dev DB's actual accumulated data (94 users, 25 audiobooks, 125 chapters, 127 words, 7 flashcards) — every count matched, and no leftover throwaway database was left behind. `packages/backend/DISASTER_RECOVERY.md` documents the tested procedure plus the honest gaps (no automated schedule or off-site copy, since no hosting provider is chosen yet — same standing gap as `deploy-staging.yml`'s own TODO).

The other 9 checklist items were verified, not rebuilt: re-ran the full golden path live against the real backend (register → login → catalog → free-tier chapters → word click → stats → flashcards → checkout's expected 503 → support ticket → beta feedback → admin analytics/content-analytics/experiments), confirming Dia 78-79's transaction fix works correctly on the real happy path too, not just its own failure-injection test. Measured real numbers rather than assuming: backend coverage (94.26% lines, `test:coverage` already existed since Dia 21) is close to the plan's 95% target; web coverage had never been measured before today — `@vitest/coverage-v8` + a new `test:coverage` script put a real number on it (56.91% lines), honestly far under 95%, consistent with this project's established pattern of verifying UI work live in-browser rather than writing a test for every component.

Not done, honestly (all pre-existing, tracked gaps compiled rather than newly discovered): no real Stripe test-mode checkout has ever run (still no Stripe account); no email-sending service exists at all (not "undertested" — there's nothing to test); no hosting provider is chosen, which is also what blocks a real production backup schedule/DR rehearsal. `LAUNCH_CHECKLIST.md`'s bottom line spells out exactly what's launch-ready versus blocking.

**Dia 81-85 is done, starting Etapa 6 (Mobile, Semanas 17-20)**. `packages/mobile` is a real Expo SDK 57 app (React 19.2.3 / React Native 0.86, `create-expo-app`'s blank template), added to the root npm workspaces as `@techspeak/mobile` with a single consolidated root `package-lock.json` (the nested one `create-expo-app` generates on its own was removed, matching how backend/web/desktop already work). Structure matches the plan's tree exactly: `App.jsx` at the package root, `src/{screens,services,store,navigation}`.

`src/services/api.js`/`AuthContext.jsx` deliberately mirror `packages/web`'s exact contract (same `apiRequest` shape, same `login(email, password)`/`register(email, password, name)`/`logout` signatures, same backend endpoints) — the mobile client talks to the real backend the same way the web app does. The one real difference is storage: `AsyncStorage` is always async (unlike `localStorage`), so `AuthContext` exposes an `isReady` flag and `AppNavigator` shows a loading spinner until the stored session has actually been read, avoiding a login-screen flash for an already-authenticated user on cold start. `AppNavigator` swaps the whole React Navigation stack based on `isAuthenticated` (Login/Register vs. a placeholder Home screen) rather than per-screen guards — the standard React Navigation pattern, and it resets navigation history on login/logout so a logged-out user can never back-navigate into a screen that needed auth. Real destination screens (audiobook catalog, player, flashcards, dashboard, chat) are deliberately not built yet — that's Dia 86-90's "Port Features" job, not this setup day's; Home is a placeholder proving the authenticated stack renders.

This environment has no Android/iOS emulator and no macOS for a real device build (an accepted gap, same category as `packages/desktop`'s "can't run `electron .`" limitation since Dia 20) — so `react-native-web` + `react-dom` (Expo's own official web target, the exact same components, not a mock) plus a new `.claude/launch.json` "mobile-web" entry were added specifically to make this genuinely verifiable through the Browser pane. Verified for real end-to-end against the live local backend: registered a real user and landed on Home; the session survived a full page reload (`AsyncStorage` persistence, not assumed); logout cleared it and returned to Login; logging back in with the same credentials worked; a wrong password showed the real backend error message inline. This also incidentally re-confirmed Dia 75's CORS restriction is doing its job — the mobile-web origin was correctly blocked by default (`Failed to fetch`), requiring a temporary `FRONTEND_URL` override in the gitignored local `.env` to test (reverted immediately after) — real native iOS/Android apps don't send an `Origin` header at all, so this restriction doesn't affect actual device usage, only this browser-based dev convenience path. `eslint.config.mjs` gained a `packages/mobile` block (browser-like globals + `__DEV__` + `process`, since neither existing preset cleanly fits React Native's runtime).

Not done, honestly: no real Android/iOS emulator or physical device was exercised (environment limitation); no push notifications yet (Dia 91-95/96-100's job); `npm audit` on the new package surfaces a moderate `uuid` advisory nested inside Expo's own build tooling (`@expo/config-plugins` → `xcode`) that would need a breaking Expo downgrade to fix — left as a tracked gap, same treatment as the pre-existing `react-router` advisory in `packages/web`.

**Dia 86-90 is done, closing Semanas 17-18 (Dias 81-90)**. All 4 plan priorities (Player, Flashcards, Dashboard, Chat IA) are real, working screens talking to the exact same backend the web app uses — plus a fifth, `AudiobookListScreen`, which isn't named in the plan but is the hard prerequisite for reaching Player at all. `AppNavigator`'s authenticated area is now a bottom tab bar (`@react-navigation/bottom-tabs`: Audiobooks/Flashcards/Dashboard) with Player and Chat pushed on top as stack screens, replacing Dia 81-85's placeholder `HomeScreen`.

`PlayerScreen` uses `expo-audio`'s `useAudioPlayer`/`useAudioPlayerStatus` hooks, remounting the audio controls per chapter (`key={chapter.id}`) so playback always starts fresh — the same pattern Dia 25 established on web after a stale-speed/volume bug. `TranscriptText`/`transcriptSegments.js` port `packages/web`'s `TranscriptDisplay.jsx` word-matching logic verbatim (`buildSegments` is pure JS, no DOM dependency), so partially-tagged transcripts render correctly here too; tapping a tagged word opens `TranslationModal` (mirrors `TranslationPopup.jsx`'s fields) and saves the click through the real `/api/user/words-learned` endpoint. `FlashcardsScreen`/`DashboardScreen`/`ChatScreen` are close ports of their web counterparts, each honestly scoped down where reasonable: Chat drops copy-to-clipboard and 👍/👎 feedback; Dashboard drops the recommendations feed; neither is a backend gap, just a smaller mobile MVP surface.

**Offline support** is deliberately much simpler than `packages/desktop`'s real SQLite sync-queue (Dia 20/61-64) — mobile has no local database wired up yet, and this doesn't queue writes made while offline (a review or progress update made offline is just not saved right now). What's real: `offlineCache.js` persists the last successful `/api/user/stats` and `/api/user/flashcards` responses to `AsyncStorage`, so Dashboard/Flashcards fall back to last-known data with an "Offline" notice instead of a bare error — verified for real by stopping the backend mid-session and confirming both screens rendered their cached data correctly.

Verified for real, end-to-end, via the `mobile-web` preview (same approach as Dia 81-85, since this environment still has no emulator): registered a user, browsed the real 25-audiobook catalog with correct lock icons, opened the free "Daily Standup" audiobook, played its real ~2s audio clip to completion (auto-marked "concluído" via `didJustFinish`), tapped a tagged word and got a real translation from `technical_dictionary`, opened the AI tutor and got a real context-aware reply quoting the actual transcript line, reviewed the word-click's auto-created flashcard through the real SM-2 endpoint, and saw all of it reflected correctly on the Dashboard. A real bug was caught this way, not just written past: `FlashcardsScreen`'s root `View` had no `ScrollView`, so the rating buttons were unreachable below the fold on a small viewport — fixed (`ScrollView` + `flexGrow: 1`) and re-verified before committing.

Not done, honestly: no real Android/iOS emulator or physical device exercised (same standing gap since Dia 81-85); no play-time word highlighting during playback (tap-to-translate only, no ticking-`currentTime` sync); offline mode is read-only (no write queue); push notifications are Dia 91-95/96-100's job, not this one's.

**Dia 91-100 is done, closing Etapa 6 (Semanas 17-20) — and with it, the entire 100-day detailed plan (Milestone 6, "Mobile ready," Semana 20)**. Both build phases hit the same real, external-account wall this project has hit repeatedly (Stripe/Dia 47, AWS S3/Dia 43, email/Dia 57-58): TestFlight and App Store Connect are gated behind a paid Apple Developer Program membership; Google Play Console behind a paid Google Play Developer account; and both behind a logged-in Expo/EAS account — none of which exist on this machine, confirmed directly (`npx eas-cli whoami` → "Not logged in") rather than assumed.

What's real: `app.json` carries genuine iOS/Android build identity (`bundleIdentifier`/`buildNumber`/`package`/`versionCode`, `com.techspeak.mobile` — a placeholder to change before a real submission) and an `expo-notifications` plugin registration; `eas.json` defines the standard `development`/`preview`/`production` build profiles. `src/services/pushNotifications.js`'s `registerForPushNotificationsAsync` (called once on login) is real, working permission-request/token-fetch code — verified not to crash on the `mobile-web` preview (`Platform.OS === 'web'` no-ops before touching any native-only API) — but the actual Expo push token fetch is gated behind a real EAS `projectId`, still a placeholder. Honestly, not silently: there's no backend endpoint to save a device's push token yet, so even with real credentials nothing would send a push today — building that send-side without ever being able to verify a real push arrives would repeat this project's own rule against half-built features.

`packages/mobile/IOS_BUILD.md`/`ANDROID_BUILD.md` document the exact `eas login` → `eas build:configure` → `eas build` → `eas submit` sequence for whenever real accounts exist, plus each platform's real caveat (Google Play Console vs. App Store Connect's different testing flows). One genuinely real, account-free path was found and documented: `npm run start:tunnel --workspace=packages/mobile` (`expo start --tunnel`) lets a real phone run this exact app via the free Expo Go client over Expo's relay — no Wi-Fi-matching, no paid account — the closest thing to real-device testing available without one. It's documented and ready but not itself exercised end-to-end from here: this sandboxed environment has no phone to scan the QR code with, and confirmed (not assumed) no Android SDK/emulator installed either.

Full backend (299 tests, 291 pass/8 known-baseline fail) and web (38 tests) suites, lint, and Prettier all stay green — no application code outside `packages/mobile` changed this phase.

**With this, every day/commit named in `PROJETO_AUDIOBOOK_PLANO_DETALHADO.md`'s detailed Dia 1-100 plan is done.** The plan's own Milestone list (Semana 25, "Public Launch 🚀") has no further day-by-day breakdown beyond this point — what remains before a real public launch is exactly [LAUNCH_CHECKLIST.md](LAUNCH_CHECKLIST.md)'s Dia 80 list plus this phase's own new gaps: real Stripe/email/hosting-provider/backup-schedule decisions, and now real Apple/Google Developer + EAS accounts. Nothing here invents next steps beyond what's honestly true.

**Post-Dia-100 (2026-08-10): real-device bug-fix round + Play Store prep**. The Google Play Developer account is created and a real MEI CNPJ exists (used for Stripe on 2026-08-06); the app has been going through real-device testing cycles ahead of the store listing. `packages/mobile/STORE_LISTING.md` has real, catalog-grounded short/full descriptions. `PlayerScreen`/`TranscriptText` gained font-size/font-family reader controls (mirroring web's `ReaderTopBar`, previously mobile-only gap).

`eas update` (OTA) was set up on Dia 91-100 but its first real-world use did not visibly apply on the user's device (undiagnosed) — reverted to full `eas build --profile preview` APK builds as the reliable path for every fix this round; OTA is not yet trusted for real shipping until it's debugged separately.

Three real bugs found via actual device testing, all mobile-only (web already correct in every case, confirmed by comparison before changing anything):

1. **Underline on every word, every book** — `TranscriptText.jsx` hardcoded `textDecorationLine: 'underline'` on every tappable word. Removed.
2. **Only one playback speed ever actually played** — root cause needed two passes. First (wrong) theory: a `status.playing` guard on `cycleSpeed()`/`toggleRepeat()` meant a tap while paused updated the label but not the native player: fixed by applying unconditionally, but the real device still only played at one rate afterward. Real cause, found by reading `node_modules/expo-audio`'s own iOS/Android native source: `playbackRate` is a **read-only** native property on both platforms (`Property("playbackRate")` has no `.set` block, unlike `loop`/`shouldCorrectPitch` which do) — `player.playbackRate = x` was always a silent no-op. Fixed by calling the imperative `player.setPlaybackRate(x)` method instead, which the native side does wire up. Web's `audioRef.current.playbackRate = x` was never a valid comparison for this one property, since HTML5 `<audio>` (and expo-audio's own web shim) genuinely expose a settable `playbackRate` — this bug was invisible in every browser-based preview and only ever showed on real hardware.
3. **Translation popup felt slow to appear** — it only opened once the full round trip (DB lookup, or on a first-ever click for that word, a real AI call) had already resolved, with zero visible feedback in between. `TranscriptText` now calls `onWordPress` immediately with a `{ loading: true }` placeholder, opening `TranslationModal` instantly with a spinner, then swaps in the real content once resolved — the actual lookup latency is unchanged, only the perceived responsiveness improved.

Also added a light/dark/system theme toggle to `PlayerScreen` itself (next to "Capítulo X de Y") — previously only `TopNavBar` (the Audiobooks/Flashcards/Dashboard tabs) had one, so the reading screen had no way to switch themes.

**Real device install troubleshooting note, for next time**: a Samsung device refused to install *any* APK (old or new build, identical signing fingerprint confirmed via `eas build:list`) with a generic "app não foi instalado" — root cause was the device's internal storage at 97% full (only ~650MB free), not anything build/code-related. If this recurs: check device storage before suspecting the build; a corrupted/incomplete download and Samsung's Auto Blocker are the other two real candidates ruled out along the way.

**Payment provider switched from Stripe to Mercado Pago (2026-08-11)**. The Stripe account's identity verification got stuck on a real bureaucratic mismatch — the user's CEP was renumbered by Correios two years ago (13240-000 → 13240-065), their only proof-of-address document shows the new CEP, and Stripe's review kept rejecting it with no resolution after a week of waiting on a support ticket. Rather than keep waiting indefinitely, the user chose to switch providers outright. Mercado Pago was picked over Appmax (the other real candidate researched) specifically because the user wanted an app to track sales from their phone, and Mercado Pago's is far more mature/widely used than Appmax's more niche one; Appmax's Reclame Aqui history also showed real fund-retention complaints, a real risk for a brand-new account.

The swap touched every Stripe integration point from Dia 47-56 (`services/paymentService.js`, `controllers/paymentController.js`, `app.js`'s webhook route, `PaymentSuccessPage.jsx`) but kept every contract the rest of the app depends on unchanged: `createLifetimeCheckoutSession` still returns `{url}`, `grantLifetimeAccess`/`refundLifetimePurchase`/`isWithinRefundWindow`/`REFUND_WINDOW_DAYS` keep their exact signatures, and the experiment-attribution round-trip (Dia 55-56's pricing A/B test) still works end to end. Migration 056 adds `users.mp_payment_id` **additively** — `stripe_payment_intent_id` (migration 050) is left in place untouched, since this app has never had a real paying Stripe customer to migrate.

Two real technical differences from Stripe, confirmed by reading `node_modules/mercadopago`'s own TypeScript definitions and Mercado Pago's official webhook spec before writing any code, not assumed from memory: (1) Mercado Pago's webhook signature (`x-signature`/`x-request-id` headers, HMAC-SHA256 over `id:<data.id>;request-id:<x-request-id>;ts:<ts>;`) is computed from headers + the notification's query string, not a raw-byte hash of the whole body like Stripe — so the webhook route no longer needs `express.raw()` before the global `express.json()`, simplifying `app.js`; (2) Mercado Pago normalizes metadata keys to snake_case server-side regardless of what's sent, a well-known footgun with this SDK — `createLifetimeCheckoutSession` sends `user_id`/`experiment_name`/`experiment_subject_id`/`experiment_variant` snake_case from the start instead of the old camelCase Stripe metadata shape, so the create/read shape actually matches. The webhook handler also now fetches the real payment resource (`Payment.get`) and only grants access when its `status` is `'approved'` — Mercado Pago notifies on every status transition (pending/rejected/approved), unlike Stripe's `checkout.session.completed` which only ever fired on success.

Also fixed while sweeping every `Stripe` reference in the codebase for ones that needed to change (most historical Dia-N narrative entries were deliberately left alone, matching this file's own convention of not rewriting dated history): `PrivacyPolicyPage.jsx` named Stripe three times as the payment processor handling card data — a real legal-accuracy issue, not just stale text, now corrected to Mercado Pago. `AdminRevenuePage.jsx`'s honest-stub copy was also independently stale (it said "cobrança ainda não foi implementada," pre-dating Dia 47 and never updated) — fixed to reflect what's actually true now. `API.md`, `PRICING.md`, `MONETIZATION.md`, `GO_LIVE_CHECKLIST.md`, and `DEPLOYMENT.md` (current-state/actionable docs) were updated; `LAUNCH_CHECKLIST.md`/`MONETIZATION_REPORT.md` (dated point-in-time reviews) and `Projeto_detalhado/PROJETO_AUDIOBOOK_PLANO_DETALHADO.md` (the original plan, already superseded on the OpenAI/Anthropic decision the same way) were deliberately left untouched.

Verified: `tests/unit/paymentService.test.js` and `tests/integration/payment.test.js` were rewritten for the new provider — the integration tests build real, correctly-signed webhook requests using the same HMAC recipe production code uses (not mocked away), only the actual Mercado Pago SDK calls (`Preference.create`/`Payment.get`/`PaymentRefund.total`) are mocked, the one real boundary uncontrollable without a live account. `PAYMENT_TROUBLESHOOTING.md` was rewritten with the exact sandbox setup steps (ngrok for the webhook, since Mercado Pago has no CLI tunnel like Stripe's `stripe listen`) and real Brazil sandbox test card numbers/cardholder-name conventions for whenever a real account exists.

**Same day, real sandbox credentials arrived and closed part of that gap**: the user created a real Mercado Pago application (Checkout Pro, "Pagamentos online") and got test credentials within the same session — `MERCADOPAGO_ACCESS_TOKEN` is now genuinely configured in this dev machine's `.env`. Calling `create-checkout-session` for real against the live Mercado Pago API immediately surfaced a real bug no mock had caught: `auto_return: 'approved'` combined with a non-`https://` `back_urls.success` makes preference creation fail outright (`400 auto_return invalid. back_url.success must be defined`) — confirmed directly against the sandbox API with both an `http://localhost` and a real `https://` back URL before touching any code, not assumed from documentation. Fixed by only sending `auto_return` when `FRONTEND_URL` is `https://` (local dev now gets a working preference without it; production gets it automatically once a real domain exists) — `paymentService.test.js` gained a dedicated test for this exact case. Re-verified live afterward: `create-checkout-session` now returns a real `mercadopago.com.br` checkout URL, and navigating to it loads a genuine Mercado Pago-hosted "Como você prefere pagar?" page showing the correct product name and price. This closes the "real Mercado Pago sandbox checkout" item `PAYMENT_TROUBLESHOOTING.md` had flagged as not yet done that same day — what's still open is a full webhook-confirmed purchase, which needs an ngrok tunnel (`notification_url` currently points at `localhost`, unreachable from Mercado Pago's servers).

This also created a new known-local-only test failure, same category as `ANTHROPIC_API_KEY` since Dia 26: the two `payment.test.js`/`paymentService.test.js` cases asserting a 503 without `MERCADOPAGO_ACCESS_TOKEN` only pass where that env var is genuinely unset (true in CI; not true on this machine anymore). Full backend suite: 341 tests, 331 pass / 10 fail — the original 8 (`ANTHROPIC_API_KEY` + no local Redis) plus these 2 new ones, no other regressions.

**Mobile purchase flow built (2026-08-12)**: mobile previously had no way to buy the Vitalício at all — `PlayerScreen`'s paywall panel told even logged-in free users "compra pelo app web por enquanto." `PricingScreen.jsx` (new) mirrors `PricingPage.jsx`, adapted for what's actually simpler on mobile: it only exists inside the authenticated stack (no anonymous mobile browsing to a pricing page the way the site has), so the logged-in user's own id doubles as the Dia 55-56 `pricing_price` experiment's subjectId for both the price shown and the price charged, with no visitor-id/localStorage system to port. The real purchase still happens in a real browser, opened via `Linking.openURL` (not a new native in-app-browser module, avoiding a dependency after this session's earlier native-crash debugging) — since there's no custom URL scheme wired up to bring the user straight back to a specific screen, an `AppState` listener refetches the user's plan whenever the app returns to the foreground while `PricingScreen` is mounted, so returning from a completed checkout picks up the real plan with no extra tap. `AuthContext.jsx` gained a `refreshUser()` (mirroring web's, added Dia 48 there) to make this possible. `TopNavBar` gained the same "Vitalício"/"Upgrade" plan badge web's `Navbar` has had since Dia 48; `PlayerScreen`'s paywall panel now shows a real "Comprar Vitalício" button for logged-in free users instead of the old dead-end copy (an anonymous visitor still sees a login prompt, correctly, since a purchase needs an account).

Verified live via the `mobile-web` preview: registered a fresh account, confirmed the "Upgrade" badge renders and navigates to `PricingScreen`, confirmed the screen shows the correct price/badge for the assigned experiment variant, and confirmed tapping "Comprar Vitalício" calls the real `create-checkout-session` endpoint and gets back a genuine `mercadopago.com.br` checkout URL (200 OK, confirmed via the network request). The actual `Linking.openURL` navigation itself couldn't be observed completing in this environment - the automated browser pane's popup blocker silently blocks a `window.open` triggered after an `await` (confirmed directly via a plain `window.open` test in the same tab), which is a `mobile-web`-preview-only artifact: real Android/iOS `Linking.openURL` calls the OS's native URL-open intent directly, with no browser or popup-blocker concept involved, so this doesn't affect the real app. No backend changes today.

Other docs:

- [IOS_BUILD.md](packages/mobile/IOS_BUILD.md) / [ANDROID_BUILD.md](packages/mobile/ANDROID_BUILD.md) — Dia 91-100's exact steps for real mobile builds once real accounts exist.
- [LAUNCH_CHECKLIST.md](LAUNCH_CHECKLIST.md) — Dia 80's honest pre-launch checklist review.
- [Audiobooks_English_Business.txt](Audiobooks_English_Business.txt) — product positioning/pitch notes.
- [CONTRIBUTING.md](CONTRIBUTING.md) — dev setup, commit/branch conventions, what CI runs.
- [MONETIZATION_REPORT.md](MONETIZATION_REPORT.md) — Dia 59-60's honest review of the plan's conversion/churn/CAC/LTV checklist.
- `arquivos_zipado_do_projeto.zip` is a duplicate archive of the same planning docs; not a code drop.

### Local database (dev)

A local PostgreSQL 17 instance is installed on this machine for backend dev, **listening on port 5433** (not the default 5432 — that port is already used by an unrelated pre-existing PostgreSQL 18 instance on this machine; do not touch that one). Credentials and `DATABASE_URL` live in `packages/backend/.env` (gitignored, not committed). Run `npm run migrate --workspace=packages/backend` after pulling new migrations.

### Commands

```bash
npm install              # installs all workspaces
npm run dev:backend      # Express API with --watch (packages/backend)
npm run dev:web          # Vite dev server (packages/web)
npm run lint             # ESLint across all workspaces
npm run format            # Prettier --write
npm run format:check     # Prettier --check (used in CI)
```

Desktop (Electron) has no root script yet — run `npm run dev --workspace=packages/desktop` after starting `dev:web` (it loads the web dev server at `http://localhost:5173` in development).

`test.yml` in CI runs `npm test --workspaces --if-present`: backend uses `node --test` (`packages/backend/tests/`), web uses Vitest + React Testing Library (`packages/web/src/**/*.test.jsx`). The two `ai.test.js` integration tests that assert a 503 without `ANTHROPIC_API_KEY` only pass where that env var is genuinely unset (true in CI; not true on this machine now that a real key lives in `packages/backend/.env`).

## Architecture (as scaffolded)

Monorepo with npm workspaces:
```
packages/backend/   Node.js + Express, PostgreSQL (pg), .env-based config
packages/web/       React 19 + Vite + React Router + Tailwind v4 (@tailwindcss/vite, no config file needed)
packages/desktop/   Electron; main process loads packages/web's dev server (dev) or dist/index.html (prod) — no separate renderer/electron-vite. Offline cache + sync queue use node:sqlite (built into Node 22+/Electron's bundled runtime, no native module build step). Renderer talks to main via the `window.techspeak` bridge (public/preload.js): auth.setSession, cache.getProgress/getFlashcards, cache.queueProgress, sync.now/onStatusChange.
packages/mobile/    React Native + Expo SDK 57, reuses the same backend as web/desktop. React Navigation (bottom tabs + native-stack); AsyncStorage for auth/offline cache (no local DB yet, unlike desktop's node:sqlite). expo-audio for playback, expo-notifications for push (client-side only - no backend push-token endpoint yet). No EAS/Apple/Google Developer accounts configured - see IOS_BUILD.md/ANDROID_BUILD.md.
```
External services: Anthropic Claude (chat/explanations — integrated, Dia 26; see `services/aiService.js`) — used instead of the plan's original OpenAI choice. Redis caches AI responses (Dia 30; see `services/cacheService.js`) — no local Redis on this dev machine, best-effort fallback if unreachable, validated via a Redis service in CI. Deepgram (pronunciation transcription) was integrated on Dia 33 and removed on 2026-07-24 at the user's request — not part of the app anymore. Mercado Pago (one-time lifetime purchase — integrated Dia 47, switched from Stripe on 2026-08-11; see `services/paymentService.js`) — no real account/credentials on this dev machine yet. Still planned but not yet integrated: ElevenLabs (TTS), AWS S3 + CloudFront (audio storage/CDN).

`deploy-staging.yml` only builds `packages/web` — the actual deploy step is a TODO pending a hosting provider decision.

Core domain model (see plan for full schema): `users` → `audiobooks` → `chapters` → `words` (clickable, timestamped, with translation/technical explanation) → `user_progress` / `flashcards` (SM-2 spaced repetition) / `chat_messages` (AI tutor, context-aware per chapter).

## Git conventions (from the plan)

Commit format: `<tipo>(<escopo>): <descrição>`, types: `feat`, `fix`, `docs`, `style`, `refactor`, `test`, `perf`, `ci`, `chore`, `security`, `content`.

Branch flow: `feature/*` / `fix/*` / `refactor/*` → `develop` → `staging` → `main`.

**Every `git commit` and `git push` requires explicit confirmation before running** — this is enforced via `.claude/settings.json` (`permissions.ask`), not just a convention.
